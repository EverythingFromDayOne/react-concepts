# reactjs-concepts — progress

Maintainer-facing tracking document. See [`README.md`](README.md) for the reader-facing entry point and [`roadmap.md`](roadmap.md) for the planning rationale.

## Legend

- ✅ Complete and reviewed
- 🟢 Drafted (React 19.2 idioms applied; awaiting review)
- 🟡 In progress
- ⚪ Queued
- ❌ Dropped / out of scope

---

## Roadmap (🟢 drafted)

`roadmap.md` drafted July 2026. Locks React 19.2 baseline, Vite 8 SPA stance (RSC fenced to end of Phase 1), article priority order (4 waves, 36 articles), recipe track order (10 tracks), and editorial conventions. **Approval checklist at the bottom of `roadmap.md` still open.**

---

## Phase 1a — foundation articles (🟡 in progress)

Waves gate delivery: finish a wave before starting the next. Approval checkpoint after the first 3 articles, then rolling delivery.

### Wave 1 — Foundations

| # | Article | Status | Notes |
| --- | --- | --- | --- |
| 1 | `foundations/thinking-in-react.md` | 🟢 | **First deliverable.** Mental-model anchor — everything references it. Opus tier |
| 2 | `foundations/jsx-and-rendering.md` | 🟢 | |
| 3 | `foundations/components-and-props.md` | 🟢 | |
| 4 | `state/state-and-usestate.md` | 🟢 | Second deliverable — snapshots, batching. Opus tier |
| 5 | `rendering/rendering-lists-and-keys.md` | 🟢 | Locks the stable-key convention |
| 6 | `foundations/conditional-rendering-and-events.md` | 🟢 | |
| 7 | `forms/forms-controlled-and-uncontrolled.md` | 🟢 | |
| 8 | `foundations/component-composition.md` | 🟢 | Composition-beats-memoization groundwork for the performance track |
| 9 | `effects/effects-and-synchronization.md` | 🟢 | Third deliverable — cleanup, StrictMode double-invoke, AbortController pattern locked here. Opus tier |
| 10 | `foundations/rules-of-react.md` | 🟢 | Framed as the contract the Compiler enforces |

### Wave 2 — Intermediate

| # | Article | Status | Notes |
| --- | --- | --- | --- |
| 11 | `rendering/how-react-renders.md` | 🟢 | Fiber, render/commit — the "under the hood" anchor. Opus tier |
| 12 | `effects/useref-and-the-dom.md` | 🟢 | ref-as-prop baseline; legacy `forwardRef` with marker |
| 13 | `state/context.md` | 🟢 | What it's for / not for; split-context pattern |
| 14 | `state/usereducer-and-state-structure.md` | 🟢 | Derive-don't-mirror lives here |
| 15 | `effects/custom-hooks.md` | 🟢 | |
| 16 | `rendering/memoization-and-the-compiler.md` | 🟢 | Compiler-first stance. Opus tier |
| 17 | `rendering/error-boundaries.md` | 🟢 | The one class use-case; `react-error-boundary` in practice |
| 18 | `rendering/portals-and-the-event-system.md` | 🟢 | |
| 19 | `effects/escape-hatches-audit.md` | 🟢 | `useSyncExternalStore`, `useLayoutEffect`, `flushSync` |

---

## Phase 1b — recipes (🟡 in progress — Wave 1 complete)

Starts once Wave 1 articles are complete, then runs in parallel with Waves 2–4. Track order per `roadmap.md` §4.

### First 3 recipes (locked)

| # | Recipe | Status | Scenario |
| --- | --- | --- | --- |
| 1 | `recipes/data-fetching/search-race-condition.md` | 🟢 | Stale "rea" response lands after "react" response and overwrites it. Fix arc: ignore-flag → AbortController → TanStack Query. The foundational recipe — heaviest expected in-degree |
| 2 | `recipes/performance/typing-lag-rerender-storm.md` | 🟢 | Controlled input in a 300-row dashboard; 180ms per keystroke. Fix arc: colocation → composition → Compiler verification. Deliberately not "sprinkle useMemo" |
| 3 | `recipes/forms-and-ux/double-submit-and-optimistic-like.md` | 🟢 | Two orders from a double-click; Like count flickers back on failure. Fix arc: form actions + `useActionState` → `useOptimistic` with rollback |

### Recipe #4 — state-management track opener

| # | Recipe | Status | Scenario |
| --- | --- | --- | --- |
| 4 | `recipes/state-management/context-rerenders-the-whole-tree.md` | 🟢 | 200-card product grid; megacontext bundling cart/wishlist/filters re-renders all 200 cards on any change, 220ms/interaction, INP 90ms→310ms. Fix arc: split by change cadence → leaf-extraction → honest structural ceiling + `useSyncExternalStore` escalation variation |

### Recipe #5 — data-fetching dedup (three-sessions-overdue clear)

| # | Recipe | Status | Scenario |
| --- | --- | --- | --- |
| 5 | `recipes/data-fetching/strictmode-double-mount.md` | 🟢 | Product page fires 2× GET and 2× POST-views per load; teammate deletes `<StrictMode>`, ships the missing-cleanup bug; prod view counts run ~1.9× actual. Fix arc: read the dev double as a fire drill → wrong fix (disable StrictMode) → abort+ignore for reads → classify writes (idempotency/relocation, `sendBeacon` in a loader) → key-dedup handover to TanStack Query. Sibling to `search-race-condition` (dedup half vs. ordering half) |

### Recipe #6 — data-fetching waterfall (unblocked by 27 + 28)

| # | Recipe | Status | Scenario |
| --- | --- | --- | --- |
| 6 | `recipes/data-fetching/request-waterfall.md` | 🟢 | Nested product-page components each fetch on render; Suspense boundaries accidentally serialize independent requests into a staircase — 2.8s on a throttled phone (vs ~900ms parallel). Escaped QA: localhost latency ~2ms hides it, skeletons masked it. Fix taxonomy: see/name (throttle) → parallelize independent (`useSuspenseQueries`) → real-dependency (collapse round-trips at the API) → route-loader flatten (`Promise.all` prefetch) → create-high/read-low → verify-the-loop. Cashes 27's prevention + 28's loader flatten + suspense serial-suspend from the bug side |

### Recipe #7 — state-management: server-state-in-a-client-store (writable since 29)

| # | Recipe | Status | Scenario |
| --- | --- | --- | --- |
| 7 | `recipes/state-management/zustand-goes-stale.md` | 🟢 | Admin inventory dashboard caches fetched stock in a Zustand store; a warehouse sync drops a SKU to 0 but the operator's tab still shows 5-hour-stale counts → oversell, refund, incident. Escaped QA: single-session/single-tab testing + the writer's own write-through `set()` masks it (store reflects the writer's own writes, misses every other writer). Fix arc: name the category error (a store lacks cache machinery — no staleness clock, dedup, cancellation, invalidation) → reject the anti-fixes (interval-poll = hand-rolled racing cache → cites search-race; refresh button = staleness-as-user's-job; write-through = the QA-passing trap) → move server state to TanStack Query (`staleTime` + `refetchOnWindowFocus` bounds multi-tab/second-writer staleness + `invalidateQueries` on mutation) → keep the client half (selections/filters) in Zustand. The state-placement funnel made concrete; article 29's mistake #1 points here. 11 pitfalls + a real when-NOT (a one-time frozen snapshot — signed quote/config — is client state, not server state). Verify-the-loop closes against the incident (two tabs converge on focus) |

### Recipe #8 — data-fetching: abandonment (completes the symptom trio)

| # | Recipe | Status | Scenario |
| --- | --- | --- | --- |
| 8 | `recipes/data-fetching/spinner-never-resolves-after-unmount.md` | 🟢 | Tabbed analytics dashboard hoists per-tab `{loading,data}` into a store; on a throttled phone a fast tab-switch mid-fetch aborts the request (correct cleanup) but the `ignore`-guarded `finally` skips the `loading:false` reset → `loading` stuck `true` in the store → remount's `if(loading)return` guard skips the refetch → spinner spins forever (needs a reload). ~1-in-20 mobile sessions. Escaped QA: localhost fetch ~4ms, so nobody navigates away mid-flight and the abort branch never runs in dev. Fix arc: name it (a `true` that never became `false`; request-derived state stored as a standalone hoisted flag with a reset path an abandoned request skips) → reject two naive fixes (cleanup-reset flashes in StrictMode + allows impossible states; drop-the-guard reintroduces the search-race) → derive loading from the request via TanStack Query (`status` can't strand, `queryFn`'s `signal` cancels; store shrinks to "which tab" per zustand-goes-stale) → hand-rolled fallback = a local discriminated-union status (destroyed with the component, restarts at `idle`). 11 pitfalls + a real when-NOT (fire-and-forget writes / can't-unmount-mid-request). Verify-the-loop closes against the incident. **Completes the data-fetching symptom trio: ordering (#1) + dedup (#5) + abandonment (#8).** |

### Recipe #9 — auth track opener: single-flight token refresh

| # | Recipe | Status | Scenario |
| --- | --- | --- | --- |
| 9 | `recipes/auth/refresh-storm-on-401.md` | 🟢 | Dashboard loads 6 widgets in parallel; access token expired during a 20-min idle tab; all 6 → 401 at once → 6 concurrent `/auth/refresh` calls. With refresh-token **rotation** (security best practice), the first refresh consumes+rotates the token, the other 5 present the now-dead token → rejected → app reads it as "session dead" → logout. ~1-in-3 post-idle loads; "it keeps logging me out." Escaped QA: tests issue requests sequentially (never collide) + rotation often only in staging/prod + needs an expired-token-at-multi-request-load. Fix arc: name it (refresh is per-request, should be per-app; rotation makes concurrent refreshes self-invalidate) → reject the naive `isRefreshing` boolean (stops extra refreshes but drops the requests that should wait/retry) → **single-flight refresh with a shared promise** (`refreshPromise ??= refreshToken().finally(()=>refreshPromise=null)`; first 401 starts it, all concurrent 401s await the SAME one, each retries its own request once) → fail-once (idempotent `onAuthFailure` = `queryClient.clear()` + redirect once, not per-request) + integrate (authFetch under Query/Router; Query `retry:false` on 401 so it doesn't 3× the storm). The auth-track equivalent of search-race (dedup concurrent in-flight work). 11 pitfalls + when-NOT (opaque server-side sessions / single-request pages). httpOnly-cookie + in-memory-access-token security stance; CSRF note. Verify-the-loop: 6 parallel calls → assert 1 refresh + 0 logouts |

### Recipe #10 — routing track opener: URL as the single source of truth

| # | Recipe | Status | Scenario |
| --- | --- | --- | --- |
| 10 | `recipes/routing/params-out-of-sync.md` | 🟢 | Product listing keeps category+page in the URL AND mirrors them into `useState` seeded from the URL on mount. Back button after clicking into a product → URL restores `?category=electronics&page=3` but the component didn't remount so the `useState` initializers never re-ran → grid shows stale state; "Clear filters" navigates the URL but not the mirror; `setSearchParams({category})` wipes `page`. "The Back button forgets my filters." Escaped QA: the initializer reads the URL on mount so navigate-to-URL-and-assert-on-load tests pass; the bug only appears on URL-change-*without*-remount (Back/Forward, query-only `<Link>`, programmatic `navigate`) which forward-driving QA never exercises. Fix arc: name it (URL is already durable/shared/history-aware state; mirroring makes a second copy with none of those properties, unsyncable by hand) → reject the `useEffect` URL→state sync (the derive-don't-mirror anti-pattern → extra render, frame of lag, loop risk; cites 14 + you-might-not-need-an-effect) → delete the `useState`, derive `category`/`page` from `params` during render, write via functional `setSearchParams(prev=>…, {replace:true})` (preserves siblings, no history spam) → parse/validate on read (string→number fallback), where TanStack Router's `validateSearch` designs the failure out (cites 35). 10 pitfalls + when-NOT (ephemeral non-shareable UI state stays local; the funnel decides). Verify-the-loop: filter→page 3→product→Back → grid matches URL. **Pays the forward-ref from article 35's See-also.** |

### Recipe #11 — auth: logout teardown (pays recipe #9's forward-ref)

| # | Recipe | Status | Scenario |
| --- | --- | --- | --- |
| 11 | `recipes/auth/logout-leaves-stale-cache.md` | 🟢 | Shared-workstation support console; agent A logs out (token cleared, `navigate('/login')`) but the root `QueryClient` lives for the tab's life, so its cache still holds A's `['tickets','queue']`/`['customer',id]` PII; agent B logs in → protected routes mount → cache-hit renders A's data to B (a cross-user data-exposure incident, not a flicker). Worse with a `persistQueryClient` persister: A's cache is on disk, survives reload/restart, re-hydrates before auth. Escaped QA: single-user testing never switches identities on one `QueryClient`. Fix arc: name it (logout is a *teardown*; the token is the credential, the cache is the data it unlocked — both are session-scoped, correctly-placed server state per the funnel, so this is an incomplete teardown not a misplacement) → `queryClient.clear()` (NOT invalidate/reset — those refetch) with the right order (`cancelQueries` first so a straggler can't repopulate → kill token → clear; guard unmounts the authed tree) → clear the persisted copy too (`persister.removeClient()` + `buster: userId`; honest default = don't persist authed queries / `gcTime:0` for PII) → propagate across tabs (`BroadcastChannel`) + share ONE `endSession` with the 401 path (#9's `onAuthFailure`). 5 variations (clear-vs-refetch verbs, reset Zustand residue too, server-side httpOnly `POST /logout`, per-user key namespacing, `gcTime:0`). 12 pitfalls + real when-NOT (single-identity device — "lock" for the same user — keeps the cache as warm-start; the test = "could a *different* person end up looking at this cache?"). Verify-the-loop: A→logout→B → assert B's cache empty + no A-keyed localStorage + other tabs redirected. **Pays recipe #9's forward-ref (the shared cache-clear-on-logout teardown); completes the auth teardown story.** |

### Recipe #12 — auth: flash-free guard (closes the auth track)

| # | Recipe | Status | Scenario |
| --- | --- | --- | --- |
| 12 | `recipes/auth/flash-of-protected-content.md` | 🟢 | Cold start on a protected URL (hard reload / bookmark / deep link): access token wiped by reload, silent refresh in flight, so first paint has `user == null`. Binary guard either renders protected content optimistically → a frame (or 300ms) of invoice PII before yanking it + redirecting, OR treats `null` as unauthenticated → redirects an authed user to `/login` then bounces them back. Escaped QA: dev `/me` resolves ~2ms (loading window = 1 frame); tests navigate forward client-side (token already in memory) so the guard never sees `loading`; needs a cold start over real latency. Fix arc: name it (auth is 3 states `loading`/`authenticated`/`unauthenticated`, not `user\|null` — collapsing `loading` into a decided branch IS the flash; cites thinking-in-react model-states-not-booleans) → SPA `RequireAuth` gates on the union (spinner while loading, `<Navigate replace state={{from}}>` when unauth, children when authed; redirect in render NOT useEffect) → **flash-free default in RR data mode: decide in a loader** (`throw redirect` runs before the protected component mounts → no flash by construction; put it on a parent layout route; cites 28) → bootstrap once + return-trip (`?next`/`location.state.from`; httpOnly-cookie SSR renders authed shell = zero client `loading` + no hydration mismatch, cites 25). 5 variations (component-vs-loader, preserve-destination, SSR-zero-loading, authorized≠authenticated 403-not-login, refresh-in-progress-isn't-logged-out → awaits #9's single-flight). 12 pitfalls + real when-NOT (public route needs no guard; SSR-always-decides-first makes client machinery redundant — test = "can first paint happen before auth is known?"). Verify-the-loop: cold-reload authed (splash→page, no flash) / logged-out (→login, no PII frame) / deep-link-logged-out (→login→land on target). **Pays recipe #9's See-also forward-ref; completes the auth trilogy (opener #9 + teardown #11 + guard #12).** |

### Recipe #13 — micro-frontends track opener (pays 37 + 38's forward-refs)

| # | Recipe | Status | Scenario |
| --- | --- | --- | --- |
| 13 | `recipes/micro-frontends/shared-react-duplicated.md` | 🟢 | Shell loads a `checkout` remote that's flawless standalone and ran composed for months; a routine `react@19.2.0→19.2.1` patch bump ships → every `/checkout` user gets `Invalid hook call`. Cause = `shared:['react','react-dom']` WITHOUT `singleton:true`: matching versions masked it, the bump let each side keep its own version → two React instances → two module-level dispatchers → the remote's `useState` reads a null dispatcher. Escaped QA: remote works standalone (one React); bug is composition-only (remote's QA never mounts it in the host); latent (wrong config shipped fine, a bump not a code change tripped it — "we didn't change anything" is true and misleading). Fix arc: name it (two Reacts, one dispatcher — NOT rules-of-hooks, cites 10; the doubled runtime, cites 38's substrates) + a concrete two-Reacts diagnostic (log `react` identity both sides / read the MF shared graph) → reject the error message's own first two suggestions + `resolve.dedupe` (single-build-only, wrong layer) → `singleton:true`+`requiredVersion` on both sides (one copy even across a patch drift) → share the WHOLE runtime kernel (react-dom/router/error-boundary/context libs — sharing react-only relocates the crash) + `strictVersion` reject-vs-warn policy + CI guard (fail build if shared graph has >1 react). 5 variations (kernel-not-just-react, transitive-React-from-a-lib peer-dep, `eager` for host baseline, dev-vs-prod divergence, native-ESM import-map = one-specifier-one-URL). 12 pitfalls + real when-NOT (not-federating → `resolve.dedupe` not `shared`; deliberately-isolated iframe/web-component → two Reacts is intentional; test = "meant to share React's runtime?"). Verify-the-loop: prod `build && preview` + re-bump → one React, mounts clean. **Opens the `micro-frontends/` recipe track (#11); pays the singleton forward-refs from both 37 and 38.** |

### Recipe #14 — micro-frontends: isolation (pays 37 + 38's failure-isolation forward-refs)

| # | Recipe | Status | Scenario |
| --- | --- | --- | --- |
| 14 | `recipes/micro-frontends/remote-failure-blanks-shell.md` | 🟢 | Shell composes 4 remotes (nav/catalog/checkout/recommendations) via `React.lazy(()=>import('checkout/Widget'))`; checkout's CDN blips → `mf-manifest.json` 404s for 3 min → **every user gets a white screen** (not "checkout down, rest works"). Cause: the failed dynamic import rejects → `React.lazy` throws → propagates to the nearest boundary; no boundary / a root boundary → React unmounts the whole tree. Blast radius inverted (least-critical remote blanks the most-critical flow). Escaped QA: remotes always up in dev/staging (failure path never runs); happy-path-only testing; availability bug only in a real prod incident. Fix arc: name it (a remote is a runtime network fetch that will fail; isolation is something you build — cites 37 `#failure-isolation-is-not-free`) → **per-remote** error boundary + Suspense (NOT root; a failed remote is a failed chunk — cites 17 `#the-stale-chunk-fallback`/`#placement-the-granularity-ladder`/`#reset-design`/`#fallbacks-stay-dumb`; `fallbackRender`+`resetErrorBoundary` for manual retry) → resilient loader wrapping the runtime `loadRemote` (`Promise.race` timeout + jittered exponential-backoff retry — `React.lazy` can't express these; cites 38 `#step-4`) → per-remote circuit breaker (threshold+cooldown, skip-load when open) + criticality tiers (recommendations=hide / checkout=loud fallback+retry / nav=full-page) + observability. 5 variations (criticality tiers, last-known-good version fallback, route-loader gating → cites 28, SSR streams the error half → cites 21, observability). 12 pitfalls + real when-NOT (single-remote-IS-the-product → full-page error correct; build-time-composed remote → can't fail at load, plain boundary; test = "can it fail at runtime AND is there other UI worth keeping alive?"). Verify-the-loop: 404 the manifest → per-slot fallback + rest renders / hang → times out / 3× fail → breaker opens / restore → retry recovers. **Pays the failure-isolation forward-refs from 37 and 38.** Includes real code (resilient loader + circuit breaker + `Remote` wrapper). |

### Recipe #15 — micro-frontends: governance (completes the MFE trio)

| # | Recipe | Status | Scenario |
| --- | --- | --- | --- |
| 15 | `recipes/micro-frontends/version-skew-breaks-host.md` | 🟢 | Checkout remote exposes `<CheckoutWidget cartId onComplete>` (host typed via `@mf-types`); the checkout team renames `onComplete`→`onSuccess` + changes its shape, updates their own callers, tests green, deploys. Host NOT rebuilt → still passes `onComplete` → new widget never fires it → **users pay and land nowhere** (silent), or throws mid-checkout. Cause: the exposed surface is a public API across a deploy boundary, and `@mf-types` is a **build-time snapshot** — independent deployment lets the runtime remote drift from the version the host built against. "Both pipelines green, combination broken." Escaped QA: host built/tested vs the remote's *previous* types; remote tested vs its own callers; no pipeline runs current-host × current-remote; types = false confidence. Fix arc: name it (exposed surface = versioned public API; types are build-time; the N·(N−1)/2 surface — cites 37 governance) → reject the safe-feeling fixes (trust-types = the false confidence; rebuild-host-every-change = re-couples the release train → cites 37 decision; pin-everything = loses independent deploy) → version the contract + **runtime** compat gate (`CONTRACT_VERSION` on the exposed surface, host checks a semver range, refuse+fallback on major mismatch → cites #14 for the fallback) → **evolve additively** (add `onSuccess`, keep `onComplete` as a deprecated alias through a window; minor not break) + a contract test in CI (cites 31 `#real-world-patterns` — catches the combination pre-deploy). 5 variations (criticality-tiered pinning, consumer-driven/Pact contracts, runtime feature detection, Zod schema on the wire, deprecation telemetry). 12 pitfalls + real when-NOT (pinned+deployed-together → no skew, and reconsider federation; single-consumer-same-team → a shared package is simpler; test = "do independent teams deploy on a different cadence than the host?"). Verify-the-loop: breaking-major→gate shows fallback / additive→un-rebuilt host keeps working / remove-while-in-use→contract test fails in CI. Real code (versioned `CONTRACT_VERSION`, runtime `satisfies` gate, deprecated-alias widget). **Completes the micro-frontends trio: singleton (#13) / isolation (#14) / governance (#15).** |

### Recipe #16 — micro-frontends: styling isolation

| # | Recipe | Status | Scenario |
| --- | --- | --- | --- |
| 16 | `recipes/micro-frontends/css-leaks-across-remotes.md` | 🟢 | Shell composes catalog + checkout; catalog ships global CSS (`* {}` reset, `button { background:navy }`, `.card {}`); checkout pixel-perfect standalone → composed, its Pay button turns navy (catalog's element selector), its `.card` gets catalog's padding (name collision), the reset shifts its spacing. **Load-order-dependent** (who wins = which stylesheet loaded later = which remote lazy-loaded first = what route you visited first) → intermittent/path-dependent. Escaped QA: standalone = only CSS on page (green); collision composition-only + order-dependent; the culprits (resets/element-selectors/generic class names) look fine in isolation. `primary_concept: ecosystem/styling-approaches` (32 owns the fix), `difficulty: intermediate` (CSS scoping is a step down from the federation-internals recipes #13–15). Fix arc: name it (CSS has no runtime module boundary — remotes compose into one document/one global namespace, cites 37 `#styling-isolation`) → reject BEM-by-convention / `!important` / load-order-coordination → scope by construction: hash class names (CSS Modules — cites 32 `#css-modules`) + ban global/element/reset selectors in remotes + shared tokens via the kernel (cites 37 `#the-shared-kernel…`) → deterministic precedence with `@layer` (shell declares order once → load-order stops mattering) + Shadow DOM for hard isolation (custom-properties pierce so theming survives; cites 32 `#real-world-patterns`) + top-layer `<dialog>` for cross-remote z-index (cites 36). 5 variations (CSS-Modules baseline, `@layer`, Shadow DOM, shared-design-system-via-kernel, Tailwind-Preflight-collision → cites 32 `#tailwind`). 12 pitfalls + real when-NOT (one-design-system-through-kernel → hashing suffices; build-time-composed → single build already scopes; test = "separately-built stylesheets meeting in one document?"). Verify-the-loop: both nav orders → identical; reset doesn't shift; shared-token change updates both. Real code (CSS Modules hashing, `@layer` order, Shadow-DOM piercing). |

### Recipe #17 — micro-frontends: cross-remote state (closes the MFE track)

| # | Recipe | Status | Scenario |
| --- | --- | --- | --- |
| 17 | `recipes/micro-frontends/cross-remote-state-coupling.md` | 🟢 | Shell + catalog + checkout + header share "the cart" via a `shared` Zustand singleton store (`@acme/cart-store` = `{items,total,addItem}`); catalog writes, header reads `.total`, checkout reads `.items`. Rots 3 ways: (1) shape coupling — checkout renames `total`→`subtotal` + deploys → un-rebuilt header reads `undefined`→`$NaN` (a store-shape change is a silent breaking change to every reader; version-skew #15 but on ad-hoc-read state, worse); (2) mount-order races — header reads before catalog populated → 0-then-flicker; (3) refactor lockstep — can't change the store in one remote without all consumers → every change a coordinated release → the independence is a lie ("cosplay"). Escaped QA: integrated dev = all remotes current + predictable mount order → coupling never fires. `primary_concept: architecture/micro-frontends` (37 owns the anti-pattern), `difficulty: advanced`. Fix arc: name it (a shared mutable store = the monolith's coupling WITHOUT its atomic-refactor safety; re-creates the funnel's problems across an un-atomic boundary — cites 37 `#cross-mfe-state-is-the-anti-pattern` + 29 `#the-placement-funnel`; and "the cart" IS server state — cites zustand-goes-stale `#stage-1`) → reject fix-the-store (strict-singleton = reliable coupling still coupled; version-the-package = documented not removed) → **versioned event + each remote owns its own read**: server truth in each remote's Query cache, actor writes server + emits `cart:changed` (event carries "changed" NOT data → checkout refactors internals freely), listeners `invalidateQueries` (cites 27 `#real-world-patterns` + `#stage-4`); mount-order race gone (fetch-on-mount) → place the rest with the funnel across the boundary (navigational→URL cites params-out-of-sync `#stage-3`; cross-cutting client→read-mostly kernel contract cites 37 `#the-shared-kernel…`; local→stays local). 5 variations (transport EventTarget/DOM-events/BroadcastChannel, server-state-is-usually-the-answer, URL, kernel-context, versioned-event-payload). 12 pitfalls + real when-NOT (one-team-one-cadence/build-time → normal app state, use the funnel; tiny-stable-write-once → kernel value no bus; test = "independent teams deploy writers+readers on different cadences?"). Verify-the-loop: add→event→header/checkout refetch (no shared store) / checkout refactors internals→header still works / late mount→fetches current. Real code (versioned `EventTarget` bus, per-remote Query+invalidate, write-server-then-emit). **Closes the `micro-frontends/` recipe track (5): singleton #13 / isolation #14 / governance #15 / styling #16 / state #17.** |

### Recipe #18 — ssr-and-rsc track opener (pays 33's See-also forward-ref)

| # | Recipe | Status | Scenario |
| --- | --- | --- | --- |
| 18 | `recipes/ssr-and-rsc/hydration-mismatch.md` | 🟢 | SSR product page renders a locale-formatted delivery date, a "3 minutes ago" relative timestamp, and a `localStorage` theme → prod console floods with `Hydration failed` + a visible flash where date/theme/timestamp snap on hydration (React 19 throws away + client-re-renders the mismatched subtree → SSR lost + a jolt). Three divergences: dates/relative-time (server render-time ≠ client hydration-time), locale/timezone (datacenter UTC ≠ user TZ/locale), `localStorage` theme (server can't read it → default; client reads stored). Escaped QA: localhost = server+client same machine (same TZ/locale, ms gap) → matches; dev overlay dismissed; only diverges in prod. `primary_concept: server/ssr-and-hydration` (25 owns the fix patterns), `difficulty: intermediate`. Fix arc: name it (hydration reuses DOM + only attaches handlers, does NOT re-render to reconcile → first render must be byte-identical; the mismatch is environment-divergent data in render — cites 25 `#the-agreement-contract…` + `#the-anti-pattern-that-guarantees-a-mismatch`) → categorize (server-deterministic: dates/locale/IDs vs genuinely-client-only: live clock/localStorage) + reject blanket `suppressHydrationWarning` (silences the warning not the divergence; one legit use, one level deep) → make deterministic values match (server-computed ISO + **server-decided locale/timezone** passed as props → identical `Intl.DateTimeFormat`; theme via **cookie** not localStorage — server-readable, zero flash, cites 33 `#real-world-patterns`; `useId` not `Math.random()` — cites 36 `#useid-and-why-not-a-counter`) → gate the client-only bits (render-same-both-passes-then-effect — cites 25 `#pattern-1…`; `useSyncExternalStore`+`getServerSnapshot` for external stores — cites 25 `#pattern-2…` + 19). 5 variations (theme-3-ways, `suppressHydrationWarning`-one-legit-use, uSES-server-snapshot, server-decided-locale, React-19-recovery+Suspense-localizes cites 21). 12 pitfalls + real when-NOT (intended-per-env value → mount-gate/narrow-suppress is correct design; not-doing-SSR → no contract, `Date.now()` fine; test = "must this render be identical on server+client?"). Verify-the-loop: server `TZ=UTC`+non-US `LANG` → US-locale client → no warning/flash, relative time upgrades post-hydration. Real code (server-locale date, cookie theme, mount-gate `PostedAt`). Leans heavily on 25 (which owns the patterns) — symptom-first framing, cites-not-reteaches. **Opens the `ssr-and-rsc/` track (#9); pays article 33's See-also forward-ref.** |

### Recipe #19 — ssr-and-rsc: the RSC boundary bug

| # | Recipe | Status | Scenario |
| --- | --- | --- | --- |
| 19 | `recipes/ssr-and-rsc/use-client-sprawl.md` | 🟢 | Next.js App Router product page; someone adds `"use client"` to `ProductPage` (or a barrel `index.ts`, or the Layout) for one favorite button's `onClick` → the ENTIRE subtree (ProductDetails/Reviews/Recommendations + their imports: markdown renderer, date/i18n lib, highlighter) becomes client → client bundle ~90KB→~400KB, server data fetching lost, secrets can't be read; the page works perfectly (secretly a client app in RSC's clothes). Escaped QA: no functional bug (all tests pass); `"use client"` always makes the error go away (path of least resistance); compounds silently (count only goes up; nobody runs the bundle analyzer). `primary_concept: server/server-components` (26 owns the boundary model — parallels hydration→25), `difficulty: intermediate`. Fix arc: name it (`"use client"` marks a **boundary** not a label — transitive downward through imports; server is default, push the opt-out as far DOWN as possible to the interactive leaf — cites 26 `#the-module-graph-split--where-zero-bundle-comes-from` + `#why-server-components-cant-be-interactive`) → find the real boundary + reject the 3 reflexes (barrel `index.ts` poisons every re-export; page/layout makes whole route client; "fix the hook error on an ancestor" → mark the leaf; diagnose via bundle analyzer — cites 34 `#real-world-patterns`) → push the boundary to the leaf (async server `ProductPage` fetches server-side + renders a `"use client"` `FavoriteButton` leaf; heavy libs stay server) → **slot pattern**: client wrapper holds server `children`/props (rendered on server, passed as RSC payload, stay server — cites 26 `#stage-2--a-server-component-inside-a-client-component-the-slot`); providers high but server children flow through. 5 variations (slot, barrel-poisoning, move-data-fetch-server-side, `"use client"` vs `"use server"`, thin-wrapper-for-3rd-party). 12 pitfalls + real when-NOT (thoroughly-interactive page → high boundary honest; not-RSC/Vite-SPA → no boundary). Verify-the-loop: bundle analyzer before/after → heavy libs gone, RSC devtools shows tree server again, `"use client"` count at leaves. Real code (server ProductPage + client FavoriteButton leaf + `ClientTabs` slot). **Pays another of article 33/26's See-also debts.** |

### Recipe #20 — ssr-and-rsc: revalidation (closes the track; pays 33's last See-also debt)

| # | Recipe | Status | Scenario |
| --- | --- | --- | --- |
| 20 | `recipes/ssr-and-rsc/server-action-stale-data.md` | 🟢 | Admin edits a product price via a Server Action; product cached with `use cache`+`cacheTag('product-'+id)`; action writes the DB + returns success → **price on the page unchanged** ("saving doesn't work" — it does, the cache serves stale). Escalating near-misses: `revalidatePath('/products')` (list not `/products/[id]`), `revalidateTag('products')` (tag ≠ `product-${id}` → silent no-op), right tag but `revalidateTag` = stale-while-revalidate so the admin STILL sees old until next nav. Escaped QA: **doesn't reproduce in `next dev`** (dev doesn't cache like prod → only `next build && next start` shows it); action succeeds → tests pass; tag string + mechanism choice silent. `primary_concept: server/nextjs-and-rsc-in-practice` (33 owns the caching/invalidation model — completes the 25/26/33 primary-concept symmetry across the 3 ssr recipes), `difficulty: intermediate`. Fix arc: name it (mutation = write + invalidate-what-it-made-stale; caching opt-in/explicit so the 2nd half is skipped; can't repro in dev — cites 33 `#step-2--a-server-action-that-mutates-and-revalidates`) → match invalidation to cache (revalidate tag = `cacheTag` string, derive both from one `productTag` helper so no drift; reject wrong-path/wrong-tag near-misses) → **read-your-own-writes: `updateTag` (Server-Action-only, immediate) vs `revalidateTag(tag,'max')` (stale-while-revalidate, background/other-users)** — using revalidateTag where you needed updateTag IS the "saved and nothing happened" report → don't-forget-the-client-cache (RSC→Query bridge = 2 caches; `invalidateQueries` too — cites 33 `#step-3--the-rsc--tanstack-query-bridge` + 27 `#stage-4…`; same lesson as zustand-goes-stale). 5 variations (the invalidation trio, one-tag-helper, client-cache-too, optimistic-UI-bridges-the-window cites 23 `#useoptimistic…` + double-submit, test-with-build-not-dev). 12 pitfalls + real when-NOT (uncached/always-dynamic → nothing to revalidate; not-RSC/Vite-SPA → ordinary `invalidateQueries`, no `updateTag`). Verify-the-loop: `next build && next start` → admin sees new price immediately (updateTag) / 2nd visitor next load (revalidate) / client cache invalidated. Real code (cached+tagged `getProduct`, `savePrice` with `updateTag`, client `invalidateQueries`). **Closes the `ssr-and-rsc/` track (3): hydration #18 / use-client-sprawl #19 / server-action-stale-data #20. Pays article 33's last See-also debt.** |

### Queued recipe candidates by track

| Track | Candidates |
| --- | --- |
| `data-fetching/` | ~~Duplicate requests on StrictMode double-mount~~ (✅ landed as recipe #5); ~~request waterfall from nested components~~ (✅ landed as recipe #6); ~~spinner never resolves after unmount~~ (✅ landed as recipe #8) — **data-fetching track symptom trio complete** |
| `performance/` | 900KB initial bundle (route splitting); 6s freeze on tab switch (virtualization); LCP regression from client-only rendering |
| `forms-and-ux/` | Validation fires on first keystroke; debounced search with loading states; multi-step wizard state survival |
| `state-management/` | ~~Server data cached in Zustand goes stale~~ (✅ landed as recipe #7); prop drilling 6 levels deep |
| `auth/` | ~~Refresh storm on 401~~ (✅ #9 — opener); ~~logout leaves stale query cache~~ (✅ #11 — teardown); ~~flash of protected content before redirect~~ (✅ #12 — guard) — **auth track COMPLETE (trilogy: opener + teardown + guard)** |
| `routing/` | Back button loses scroll position; lazy route flashes blank; ~~URL params out of sync with component state~~ (✅ landed as recipe #10 — routing track opener) |
| `testing/` | Flaky `waitFor` tests; `act()` warnings; MSW handler leaks between tests |
| `real-time/` | Double WebSocket connections in dev (StrictMode); missed reconnection; stale closure in message handler |
| `ssr-and-rsc/` | ~~Hydration mismatch on dates/random values~~ (✅ #18); ~~`"use client"` sprawl~~ (✅ #19); ~~server action returns stale data~~ (✅ #20) — **track COMPLETE (3: hydration / use-client-sprawl / server-action-stale-data)** |
| `pwa-offline/` | Cached shell serves stale app; offline mutation queue |
| `micro-frontends/` | ~~Duplicate React (singleton)~~ (✅ #13); ~~remote failure blanks the shell (isolation)~~ (✅ #14); ~~version-skew breaks host (governance)~~ (✅ #15); ~~CSS leaks across remotes (styling)~~ (✅ #16); ~~cross-remote state coupling~~ (✅ #17) — **track COMPLETE (5 recipes: singleton/isolation/governance/styling/state)** |

### Planned composition hubs

Dense cross-referencing is a locked philosophy (composition IS content). Expected heaviest in-degree, mirroring what worked before:

- **`search-race-condition`** — every async recipe references it
- **`typing-lag-rerender-storm`** — hub for the performance track
- **`effects-and-synchronization` (concept)** — referenced by every recipe touching lifecycle
- **`data-fetching-tanstack-query` (concept)** — referenced by data-fetching, auth, and state-management tracks

---

## Phase 1c — advanced + ecosystem articles (🟢 all drafted)

### Wave 3 — Advanced

| # | Article | Status | Notes |
| --- | --- | --- | --- |
| 20 | `concurrent/concurrent-rendering.md` | 🟢 | Transitions, `useDeferredValue`, tearing. Opus tier |
| 21 | `concurrent/suspense.md` | 🟢 | Pending-is-a-boundary; ErrorBoundary-outside/Suspense-inside triad; stale-chunk reset |
| 22 | `concurrent/use-and-promises.md` | 🟢 | `use()` promise-tracking trace; stable-promise rule; await-vs-use; `.catch()` graceful degradation |
| 23 | `concurrent/actions.md` | 🟢 | The 19 mutation story; pairs with the double-submit recipe |
| 24 | `rendering/react-compiler-deep-dive.md` | 🟢 | Includes the Vite 8 / `@rolldown/plugin-babel` wiring gotcha. Opus tier |
| 25 | `server/ssr-and-hydration.md` | 🟢 | Framework-agnostic fundamentals. Opus tier |
| 26 | `server/server-components.md` | 🟢 | The RSC model. Opus tier |

### Wave 4 — Ecosystem

| # | Article | Status | Notes |
| --- | --- | --- | --- |
| 27 | `ecosystem/data-fetching-tanstack-query.md` | 🟢 | Wave 4 opener (428 lines). v5 API verified against official docs before writing. Paid ALL owed debts: server-cache spine, `useSuspenseQuery` as production stable-promise source, `useSuspenseQueries` waterfall prevention, `QueryErrorResetBoundary`+react-error-boundary, server-persisted-cart (optimistic `onMutate`/`onError`/`onSettled`), client-cache-vs-RSC contrast, raw-fetch-in-useEffect failure mode. Gates the data-fetching recipe track's library stage |
| 28 | `ecosystem/routing-react-router.md` | 🟢 | Data mode (395 lines). v7 APIs verified against official docs (defer-wrapper removed → bare-promise deferral, granular route `lazy`, `react-router` package consolidation, two error channels). Paid: `errorElement`/loader-error integration + the errorElement-vs-React-boundary distinction (error-boundaries), `<Await errorElement>` deferred-rejection (suspense), loaders/actions/deferred data, route-`lazy` code-split vs React.lazy (suspense). Bonus: RR `<Form>` vs React-19 `<form action>` disambiguation table; loader-prefetch-into-Query integration |
| 29 | `ecosystem/state-management-landscape.md` | 🟢 | Decision table, not a tour (277 lines). Zustand v5 / Jotai v2 / RTK v2 surfaces verified against official docs (esp. Zustand v5 object-selector-crashes-without-`useShallow`). Spine = the placement funnel (server→Query, form→Actions, config→context, local→useState; residue = cross-cutting client state). Paid the selector-store decision-table debt (context + state-mgmt recipe both point here) via the subscription-granularity mechanics + the decision table. Bonus: RTK-Query-vs-TanStack honesty (same category, don't run two caches), persistence/hydration caveat. Walkthrough added in depth pass (routing one screen's state end-to-end). Depth pass 2 (Huy coverage review, →279): added the derived-state axis across all three (informs the Jotai-for-derived-heavy cell) + the concrete Zustand-vs-Jotai call (atomic model wins for many-independent-pieces) + an async-is-usually-server-state guard. Deliberately did NOT add Redux-thunk/async-atom tours (would fight decision-not-tour + the spine) |
| 30 | `forms/forms-at-scale.md` | 🟢 | RHF v7 + Zod v4 (280 lines). Both surfaces verified against official docs (Zod v4: `z.email()` not `.email()`, `error` not `message`, `.issues` not `.errors`, `.refine`+`path` for cross-field; RHF: uncontrolled `register`, `Controller`, `useFieldArray` keyed by `field.id`, `zodResolver`). Paid the Actions-vs-RHF boundary debt (submission-complexity vs field-complexity table) + forms-controlled's Zod/RHF threshold + field-arrays/cross-field. Walkthrough: account form (field array + cross-field refine + shared schema client&server, server-only error via `setError`). Included from the start this time; self-audit added the success path (`reset`) |
| 31 | `ecosystem/testing.md` | 🟢 | Vitest 3 + RTL v16 + user-event v14 + MSW v2 (281 lines). All four surfaces verified against official docs (esp. MSW v2 `http`/`HttpResponse.json`/`setupServer` — v1's `rest`/`res(ctx.json)` fully gone; user-event v14 `setup()`-then-`await`). Thesis = test behavior not implementation. Paid: the toolchain-stance debt + custom-hooks' `renderHook`-full-story, query-by-role-first (the priority ladder + 3 query axes), compiler-transparent-to-tests. Walkthrough: async UserList tested end-to-end (loading→list→error-override→retry) with all four tools. Self-audit added the what-to-test/integration-first strategic beat + when-NOT (RTL vs E2E). Gates the testing recipe track |
| 32 | `ecosystem/styling-approaches.md` | 🟢 | Decision table, not a tour (233 lines). Tailwind v4 (CSS-first `@theme`/`@import`/`@tailwindcss/vite`) + the CSS-in-JS retreat verified against official docs (styled-components maintenance mode Mar 2025; RSC-not-SSR incompatibility; zero-runtime = vanilla-extract/Panda/StyleX). Axis = build-time vs runtime. Walkthrough: one Button three ways (Modules/Tailwind/vanilla-extract). Honest retreat section (not dead, still fits client-only/RN/runtime-theming). Self-audit added responsive + dark-mode-across-approaches. No hard debts owed; decision-table framing honored. Depth pass 2 (Huy coverage review, →237): added variant-management-at-scale (cva/tailwind-variants — the real Tailwind maintainability factor) + the organizational/token-enforcement decision axis + container-queries as a wash |
| 33 | `server/nextjs-and-rsc-in-practice.md` | 🟢 | Next.js 16 + React 19.2 (440 lines). App Router caching model verified against official Next.js docs before writing (the big shift: caching is opt-in / dynamic-by-default; Cache Components = PPR + `use cache`; `cacheLife`/`cacheTag` stable; `updateTag` Server-Action-only read-your-own-writes vs `revalidateTag(tag,'max')` stale-while-revalidate vs `revalidatePath`; async `params`; framework-owned React Compiler; Turbopack default; `middleware`→`proxy`). Paid all owed debts: production RSC (App Router/caching/revalidation), production SSR + PPR (static-shell-plus-stream three tiers + the "uncached data outside Suspense" enforcement), the Next.js double-compile note (framework owns the compiler wiring — cited 24), Server Actions in the App Router (cited 23 for the client half), the framework-mode routing note (cited 28). Walkthrough = a production product page end-to-end (three tiers on one page → Server Action + `updateTag` → RSC→TanStack-Query hydration bridge — cited 27). State-placement spine held across the server boundary (server state → Query cache, never a client store). Self-audit added: `@/lib/*` infra-elision note, a Route-Handler gloss, and the route-level `loading.tsx`/`error.tsx` conventions. Gates the ssr-and-rsc recipe track |
| 34 | `ecosystem/performance-profiling.md` | 🟢 | Heaviest remaining debt (439 lines). React 19.2 Performance Tracks + React Scan + INP/`web-vitals` + `<Profiler>` API all verified against official/current docs before writing. Two measurement layers (React: DevTools Profiler / `<Profiler>` / React Scan / 19.2 tracks — browser: CWV via `web-vitals`). Thesis = measure before optimizing; count-vs-cost distinction. Paid all owed debts: ecosystem-depth Profiler workflow (memoization's verification was the preview), measuring-whether-concurrent-features-helped (Scheduler track Blocking-vs-Transition lane check) + measuring-whether-the-Compiler-helped (✨ badge + `actualDuration`-vs-`baseDuration`), the React Scan triage layer, and the INP workflow (typing-lag recipe's INP tie-in). Walkthrough = the measurement loop end-to-end on a 500-row filtered table (triage → locate/cost → confirm impact via Scheduler track + INP 342ms→180ms → one fix `useDeferredValue` → verify the lane moved → confirm Compiler's ✨ half). Deliberately did NOT re-teach the typing-lag colocation fix (cited recipe as owner) or the memo/Compiler mechanism (cited 16/24) — this article owns *measurement*. Self-audit added the missing `Product` type to the walkthrough. Hub for the performance recipe track |
| 35 | `ecosystem/tanstack-router.md` | 🟢 | Type-safe alternative (331 lines) — **closes the 36-article concept track (all 36 now drafted).** TanStack Router v1 surface verified against official docs before writing: file-based routing + generated `routeTree.gen.ts`, `createFileRoute` path-literal-as-type-anchor, the `Register` module augmentation, `validateSearch` typed search params with Zod (`.catch()`-vs-`.default()`), `loaderDeps`, `beforeLoad` guards + `throw redirect`, the Query integration via typed route context + `ensureQueryData` + `queryOptions`/`useSuspenseQuery`, the `tanstackRouter` Vite plugin (named export from `@tanstack/router-plugin/vite`) ordered-before-`@vitejs/plugin-react` gotcha, and the honest TS-inference cost (search-union check + loader-return-inference `await`-don't-return trick). No hard debts owed; paid the one soft ref — 28's loader-prefetch-into-Query with typed route context. Framed as decision-focused (roadmap: one article, RR stays the baseline): headline = search-params-as-typed-state (vs RR's stringly-typed `useSearchParams`); explicit when-it-beats-RR + when-it-doesn't. Walkthrough = typed search-param filters + a Query loader (queryClient-in-context → `validateSearch` Zod schema → `loaderDeps` → `ensureQueryData` prefetch → `useSuspenseQuery` read, all typed end-to-end). Contrasted RR (28) without re-teaching; cited 27 (Query), 21 (Suspense/code-split), 30 (Zod), 33 (TanStack Start vs Next.js). Self-audit added the required Vite-plugin setup (every example depends on the generated tree) with the plugin-order gotcha |
| 36 | `ecosystem/accessibility-in-react.md` | 🟢 | Wave-4 flexible-tail article (369 lines) — with this, 35/36 concept articles are drafted (only 35 `tanstack-router` remains). React 19 `inert` boolean prop + native `<dialog>` (2026 default) + the headless-lib landscape (React Aria / Radix) verified against current docs before writing. Three pillars: semantics-first / focus management / announcements. Paid ALL owed debts: focus-trap depth (cited 12's containment embassy) + full modal a11y via portals (cited 18) — the modal walkthrough is native-`<dialog>`-first, then custom-portal-with-`inert`-trap + focus-restore; testing's tests-as-a11y-check in depth (role queries + `jest-axe`, cited 31); styling's semantics-orthogonal-to-styling (cited 32); forms' `role="alert"`/`aria-invalid`/`aria-describedby` field errors (cited 30/7). Also: ARIA-in-JSX naming (`aria-*` hyphenated, `htmlFor`/`tabIndex`), `useId` (SSR/portal-safe, cited 25), live regions (empty-then-fill), route-change focus (cited 28), `:focus-visible`, reduced-motion, don't-hand-roll-widgets. Self-audit added the `.sr-only` clip-pattern definition (was a dangling reference; `display:none` would hide from AT too). Production stance = reach for React Aria/Radix + test with a real screen reader |

---

## Phase 1d — Architecture (Wave 5, micro-frontends) (🟡 in progress — additive)

Additive extension approved July 2026 (session 6). Decision-focused; leads with when-NOT. New top-level `architecture/` folder.

| # | Article | Status | Notes |
| --- | --- | --- | --- |
| 37 | `architecture/micro-frontends.md` | 🟢 | **The decision** (224 lines) — org pattern not technical upgrade; the composition-style taxonomy (build-time/monorepo · browser-runtime MF/import-maps · server/edge · routing-layer/Multi-Zones · web components · iframes) traced by *where composition happens*; the decision funnel + table + org-scale threshold (~4+ FE teams); version-skew tax (N·(N−1)/2); shared-kernel contract; cross-MFE shared state as the anti-pattern (the placement funnel across the deploy boundary); failure isolation (error boundary + resilient loader, cites 17's granularity-ladder + stale-chunk-fallback); the SSR/RSC intersection (cites 26/33). "Basic usage" renamed **Minimal shapes** (decision-table convention). 10 mistakes ("architecture without autonomy is cosplay"), how-this-evolved (monolith→MF→MF2.0→native-ESM→Server-MF/RSC), 3 exercises. MF tooling verified July 2026 (`@module-federation/vite`, MF 2.0/manifest, Rolldown-native landing, Multi-Zones, the honest when-NOT consensus). Zero Angular. Anchors verified against real files; 5 forward-refs to 38 resolve when it lands |
| 38 | `architecture/module-federation.md` | 🟢 | **The practice** (337 lines) — MF 2.0 on Vite 8 (`@module-federation/vite`): host/remote, `exposes`/`remotes`/`shared` singletons, `mf-manifest.json`, DTS type-sharing (`@mf-types`, no `declare module` shims), dynamic remotes via the runtime API (`registerRemotes`/`loadRemote`), per-remote error boundaries. **The crown-jewel under-the-hood traces BOTH substrates** (bundler-runtime + share-scope `loadShare` vs native-ESM + import-map identity) and why each imposes the same async-boundary ordering for opposite reasons — the depth parity with the angular-concepts mechanism reflection. Walkthrough builds shell+checkout end-to-end (typed exposes → singleton proof `Invalid hook call` → failure isolation → Vite plugin gotchas: no `manualChunks`, remote-must-be-built, plugin order). Cites 37 (decision/governance/anti-pattern), 17 (stale-chunk-fallback + granularity-ladder), 29 (funnel), 24 (Vite wiring), 25 (SSR boundary). 10 mistakes, reference table, how-this-evolved, 3 exercises. Self-audit caught a self-link slug bug (`#failure-isolation…` → `#step-4--…`) pre-delivery. All anchors verified |

## Phase 2 — bridge layer (❌ deferred)

Additive bridge articles/annotations for Angular developers. Explicitly out of scope until Phase 1 matures. Zero rewrites of Phase 1 content; Phase 1 articles and recipes contain no Angular references anywhere.

---

## Locked editorial conventions

### Article structure (concept articles) — v2, July 2026 depth revision

1. Frontmatter (`article_id`, `concept_folder`, `wave`, `related`, `react_baseline`, `status`)
2. Lead-with-this callout (one-paragraph hook)
3. What it is
4. How it works under the hood (real internals, traced — fiber shapes, compile output, comparison semantics)
5. Basic usage (complete and runnable, imports included)
6. Walkthrough — build something small end-to-end (progressive steps, full files)
7. Real-world patterns
8. API/type reference table (when applicable)
9. Common mistakes (7–10, with code)
10. How this evolved (when applicable — anchored to the class → hooks → concurrent → 19 Actions → Compiler arc)
11. Exercises (2–3, with hints)
12. Summary
13. See also
14. References (official docs)
15. Demo source

Coverage bar, not line quota: every template section earning its place; typical range 380–650 lines, with length as a smell test only. Old-vs-new dual examples appear only where React itself has legacy forms: `forwardRef` vs ref-as-prop, class error boundaries, pre-Actions form handling, manual memoization vs Compiler.

### Recipe structure

1. Frontmatter (`recipe_id`, `primary_concept`, `difficulty`, `react_baseline`)
2. "What you'll build" callout (scenario summary)
3. The scenario (concrete failure mode, with numbers)
4. Walkthrough (multi-stage if complex)
5. Variations
6. Trade-offs and common pitfalls (incl. "when NOT to use this"; 10–15 concrete pitfalls)
7. See also (≥3 items)
8. References
9. Demo source

### Code conventions

- Function components only; classes appear once (error-boundaries article), immediately wrapped by `react-error-boundary`
- TypeScript strict throughout; no `React.FC` — plain function with a typed props interface; discriminated unions for variant props
- Effects contract: every subscription-like effect returns a cleanup; every fetch inside an effect uses `AbortController` and ignores aborts; effects synchronize with external systems — never derive state
- Derive during render, never mirror props into state (`useEffect`-sets-state shown only as the anti-pattern)
- Keys are stable IDs (`key={item.id}`), never array index
- `ref` as a prop (19+) over `forwardRef`; legacy form shown with marker
- Compiler-first memoization: pure components, Compiler assumed ON for app code; `memo`/`useMemo`/`useCallback` taught as mechanism, reserved for library code and non-compiled boundaries — no cargo-cult memoization in examples
- State placement rule: local UI state → `useState`/`useReducer`; server state → TanStack Query (never in a client store); cross-cutting client state → Zustand; static config → context
- Mutations: form actions + `useActionState`/`useOptimistic` where forms are involved; TanStack Query mutations elsewhere
- Testing: Vitest + RTL + user-event + MSW; query by role first, `data-testid` fallback only
- Named exports; co-located test files (`Component.test.tsx`)

### Legacy code preservation

Show old vs new patterns side by side with the marker comment:

```tsx
// legacy: written for React <19 — modernized in the upgrade pass
```

### Cross-referencing

- "See also" section at each recipe's end lists ≥3 related items
- Inline links use `[link text](../path/to/file.md#section-anchor)`
- Composition IS content — recipes that compose reference each other in the walkthrough, not just in "see also"

---

## Open questions / TODOs

- [ ] **Roadmap approval** — checklist at the bottom of `roadmap.md`; gates everything
- [ ] `LICENSE` — MIT assumed (matching angular-concepts); file not yet created at repo root — confirm choice
- [ ] `getting-started.md` / `typescript-for-react.md` — not in the 36-article roadmap; decide whether to add as pre-Wave-1 onboarding docs or fold into `thinking-in-react`
- [ ] Demo hosting — StackBlitz vs CodeSandbox vs local `demos/` folder for recipe demo sources
- [ ] Model allocation — Opus flags marked in the tables above (8 articles); confirm the rest as Sonnet-tier
- [ ] `recipes/index.md` longer-form index — revisit once ≥10 recipes exist
- [x] **Cross-link anchors — VERIFIED & FIXED (session 6).** Cloned the real repo, GitHub-slugified every heading, checked all 90 anchored internal links. All 83 broken anchors resolved: ~57 corrected in-place (exact slug + repoint-to-nearest); the remaining ones were made precise by two small structural improvements — added a `### The placement funnel` heading to `state-management-landscape.md` (its 6 inbound `#the-placement-funnel` refs were correct as authored; the heading was just missing) and promoted `error-boundaries.md`'s bold-lead sub-blocks to `###` headings (which resolved `#unwind-mechanics`→`#the-unwind`, `#placement-granularity-ladder`→`#placement-the-granularity-ladder`, `#reset-design` (now matches), `#the-19-root-error-options`→`#react-19s-root-level-reporting`, `#errors-as-state-vs-errors-as-thrown`→`#the-trycatch-redirect-paid`, and brought that article into corpus heading-consistency). The ~20 remaining repoints (`#semantics`, `#usesearchparams`, `#the-query-priority-ladder`, etc.) point at the nearest section — kept as-is by design (no heading manufactured per cross-ref). ⚠️ **One hard bug still needs a `git mv`: `useref-and-the-dom.md` lives in `rendering/` but its own `concept_folder`, progress.md, and all ~19 inbound links say `effects/` → `git mv rendering/useref-and-the-dom.md effects/useref-and-the-dom.md`.**
- [x] **Recipe `related` block shape — CONFIRMED from real originals (session 6).** Recipes **do** appear in a recipe's `related` block, prefixed **`recipes/`** (the originals — search-race, double-submit, typing-lag — all do this). Cleanup: #5/#6 listed recipe refs *without* the prefix (fixed in patch); #7–#10 dropped recipe refs from `related` (re-added their See-also siblings, prefixed `recipes/`, via the Cursor step). Note: originals order `related` *before* `difficulty`; handoff spec + session-4/5 recipes order it *after* — harmless (YAML order-independent); align new recipes to the handoff order, don't rewrite the originals.
- [x] **`difficulty` vocabulary — CONFIRMED (session 6):** `foundational` (originals 1–4) / `intermediate` / `advanced`. Session-5 assignments valid.
- [x] **Template "Basic usage" → "Minimal shapes" (articles 29 & 32) — KEEP, now a documented micro-convention:** decision-table articles use *Minimal shapes* (they show minimal shapes of competing options, not basic usage of one API). Confined to the two decision-table articles; add one line to roadmap §5 conventions to formalize.

---

## Session log

- **Session 1 (July 2026):** roadmap approved; README/progress scaffolded; **Wave 1 complete** (articles 1–10, ~4,300 lines); **locked recipe trio complete** (search-race-condition, typing-lag-rerender-storm, double-submit-and-optimistic-like, ~875 lines); template v2 depth revision + coverage-bar reframe locked mid-session; session closed with `handoff-session-2.md` (ownership map + forward-reference debt registry). Next session opens Wave 2 with `rendering/how-react-renders.md`.
- **Session 2 (July 2026):** **Wave 2 complete** — `rendering/how-react-renders.md` (480 lines, Opus tier), `effects/useref-and-the-dom.md` (536 lines), `state/context.md` (397 lines), `state/usereducer-and-state-structure.md` (433 lines), `effects/custom-hooks.md` (385 lines), `rendering/memoization-and-the-compiler.md` (356 lines, Opus tier), `rendering/error-boundaries.md` (364 lines), `rendering/portals-and-the-event-system.md` (356 lines), `effects/escape-hatches-audit.md` (390 lines) delivered. All 19 articles (articles 11–19) drafted. **State-management recipe track opened** — `recipes/state-management/context-rerenders-the-whole-tree.md` (218 lines) delivered, closing the recipe-interleave debt. Session closed with `handoff-session-3.md` (full ownership map, articles 1–19 + recipe 4; Wave 3 debt registry). Next session opens Wave 3 with `concurrent/concurrent-rendering.md`.
- **Session 3 (July 2026):** **Wave 3 complete** — all 7 articles (20–26, ~2,360 lines; 4 Opus-tier): `concurrent/concurrent-rendering.md` (430 lines, Opus), `concurrent/suspense.md` (~340 lines), `concurrent/use-and-promises.md`, `concurrent/actions.md`, `rendering/react-compiler-deep-dive.md` (Opus, Vite 8 `@rolldown/plugin-babel` + `eslint-plugin-react-hooks` v6 wiring verified against official docs before writing), `server/ssr-and-hydration.md` (Opus), `server/server-components.md` (Opus). Walkthrough confirmed as the single biggest quality lever (self-audit caught the absent SSR entry files, an abstract RSC payload, an unshown `.catch()`, a missing await-vs-use line — all filled). README refreshed to current state. Session closed with `handoff-session-4.md` (ownership map for all 26 articles + 4 recipes; Wave 4 forward-reference debt registry). Next session opens Wave 4.
- **Session 4 (July 2026) — in progress:** **Overdue recipe cleared first.** `recipes/data-fetching/strictmode-double-mount.md` (~205 lines) delivered — three-sessions-overdue dedup recipe, writable since article 9, needs nothing from Wave 4. Owns the *dedup* half of the data-fetching story (sibling to `search-race-condition`'s *ordering* half): reads the dev double-mount as a StrictMode fire drill, rejects the disable-StrictMode anti-fix, applies effects-and-synchronization's abort+ignore to reads, classifies the write path (idempotency vs. relocation, `sendBeacon`-in-loader shown), and hands off to TanStack Query's key-dedup (deferred deep-dive to article 27). Self-audit filled the Stage-3 relocation hand-wave with a concrete loader snippet. **Wave 4 opened** — `ecosystem/data-fetching-tanstack-query.md` (428 lines) delivered as article 27, the heaviest-debt article in the project. v5 API surface verified against official TanStack Query docs before writing (the `loading→pending`/`isLoading→isPending`/`cacheTime→gcTime`/`useErrorBoundary→throwOnError`/`keepPreviousData→placeholderData` rename table, the two-axis `status`×`fetchStatus` machine, the `onMutate`/`onError`/`onSettled` optimistic contract, stable `useSuspenseQuery`/`useSuspenseQueries`/`usePrefetchQuery`, `QueryErrorResetBoundary`). Traced the real cache internals (`QueryClient → QueryCache → Query` + observers, hash-keyed dedup, staleness lifecycle, structural sharing) as the "under the hood." End-to-end walkthrough: raw-fetch → `useQuery` → dedup proof → mutation+invalidate → optimistic+rollback. Paid all seven Wave-4 debts owed to this article (see row 27 note). Self-audit added the three principal-reviewer gaps: dependent queries (`enabled`), a DevTools pointer, and the mandatory when-NOT beat. — `ecosystem/routing-react-router.md` (395 lines) delivered as article 28. React Router v7 **data mode**; v7-specific APIs verified against official docs before writing (the `defer()` wrapper is removed → return an object with bare promise keys; `<Await>`/`use()` unwrap; granular route `lazy` 7.5+; package consolidated to `react-router`; the two-error-channel model). Traced the navigation lifecycle (match → parallel loaders → wait-for-critical → commit + `useNavigation` state + auto-revalidation). End-to-end walkthrough: loader with params + deferred reviews → `<Suspense>`/`<Await>` → `<Form>`/action + pending + redirect → route `lazy`. Paid all four owed debts (errorElement/loader-error + the errorElement-vs-React-boundary distinction, `<Await errorElement>`, loaders/actions/deferred, route-lazy-vs-React.lazy). Self-audit added nested-routes/`<Outlet>` (was referenced but unshown), a when-NOT beat, and `useSearchParams`. Included a bonus RR-`<Form>`-vs-React-19-`<form action>` disambiguation table (prevents a real conflation footgun) and the honest loader-prefetch-into-Query integration. **Two Wave-4 articles done (27–28); 8 remain (29–36).** — **Recipe interleaved.** `recipes/data-fetching/request-waterfall.md` (206 lines) delivered as recipe #6, unblocked by 27 + 28. Nested fetch-on-render + Suspense-serializes-independent-fetches scenario (2.8s throttled → ~900ms parallel; escaped QA via localhost latency + skeleton masking). Fix taxonomy: throttle-to-see + name-the-two-causes → `useSuspenseQueries` for accidental waterfalls → real-vs-accidental test + `enabled` dependent-query + API round-trip collapse for real dependencies → route-loader `Promise.all` prefetch → create-high/read-low → verify-the-loop. Post-delivery depth pass (Huy flagged 183→206): filled Stage 2 (`enabled` + real-vs-accidental test), Stage 3 (prefetch/ensure/stream tradeoff), pitfalls 10→13. — `ecosystem/state-management-landscape.md` (277→279 lines) delivered as article 29 (decision-not-tour; Zustand v5 / Jotai v2 / RTK v2 verified; placement-funnel spine; walkthrough added in depth pass; Huy coverage review added derived-state axis + Zustand-vs-Jotai call + async-guard). — `forms/forms-at-scale.md` (280 lines) delivered as article 30 (RHF v7 + Zod v4 verified; Actions-vs-RHF boundary; shared-schema client+server walkthrough). — `ecosystem/testing.md` (281 lines) delivered as article 31 (Vitest 3 + RTL v16 + user-event v14 + MSW v2 verified; test-behavior-not-implementation; async UserList walkthrough; gates the testing recipe track). — `ecosystem/styling-approaches.md` (233→237 lines) delivered as article 32 (decision-not-tour; Tailwind v4 + CSS-in-JS retreat verified; build-time-vs-runtime axis; one-Button-three-ways walkthrough; Huy coverage review added variant-management + org/token axis + container-queries). **Session 4 tally: 8 deliveries (recipe #5, articles 27–32, recipe #6). Wave 4 at 6/10 (fixed-order block 27–32 DONE; 33–36 remain). Recipes at 6.**
- **Session 4 coverage review (Huy-initiated):** flagged Wave-4 articles trending short. Filled real decision-relevant gaps in 29 & 32; deliberately did NOT pad (rejected Redux-thunk tour, async-atom section, store-testing as decision-not-tour / spine / other-owner violations). **Process correction for 33–36: raise the first-pass self-audit bar so it stops being rescue work** — audit each section against "what would a principal reviewer expect here" *before* delivery.
- **Frontmatter correction (Huy-flagged):** all six Wave-4 articles (27–32) had numeric `article_id` — WRONG; fixed to filename slugs. Open item: canonical `status` block shape awaited.
- **Session 4 closed.** `handoff-session-5.md` created. Final state: Waves 1–3 complete (26 articles); Wave 4 at 6/10; 6 recipes drafted.
- **Session 5 (July 2026) — in progress:** Opened the Wave-4 tail. **Canonical `status` shape locked from a real Wave-1 file** (`effects-and-synchronization.md` frontmatter, via screenshot): `status:` → `drafted: <bool>` / `reviewed: <bool>` — NOT the session-4 invented `stage:` form. Applied to the new article; existing files' status blocks fixed by Huy directly. Uploads were empty this session (no on-disk reference/roadmap/progress files — only in-context text), so `progress.md` was regenerated from the in-context copy with just the row-34 flip + this log entry; reconcile if other manual edits exist. — `ecosystem/performance-profiling.md` (439 lines) delivered as article 34 — the heaviest remaining forward-debt and most-pointed-to Wave-4 article. All four version-sensitive surfaces verified against official/current docs before writing: **React 19.2 Performance Tracks** (Scheduler track's Blocking/Transition/Suspense/Idle subtracks + Components flamegraph; dev-auto vs profiling-build behavior; cascading-update detection), **React Scan** (visual necessary-vs-unnecessary triage; `scan()` / CLI / script-tag; dev-only; `dangerouslyForceRunInProduction` never), **Core Web Vitals** (INP good ≤200ms / poor >500ms, replaced FID Mar 2024, = input-delay+processing+presentation; LCP 2.5s/4s; CLS 0.1/0.25) + the **`web-vitals`** API (`onINP`/`onLCP`/`onCLS`, attribution build, `sendBeacon`, SPA hard-navigation caveat, LoAF attribution), and the **`<Profiler>`** `onRender` signature (6-arg; the 7th `interactions` arg removed in React 18). Thesis = measure at two layers (React: what/why/how-much rendered; browser: what the user felt), never optimize before measuring; the count-vs-cost distinction is the spine. Paid ALL owed debts: the ecosystem-depth Profiler workflow (memoization's verification was the preview), measuring-whether-concurrent-features-helped (Scheduler track Blocking-vs-Transition lane check) + measuring-whether-the-Compiler-helped (✨ badge + `actualDuration`-vs-`baseDuration`), the React Scan triage layer, and the INP workflow (typing-lag recipe's INP tie-in). Walkthrough (highest-leverage section, ~190 lines) = the measurement loop end-to-end on a 500-row filtered table: React Scan triage → Profiler locate+cost (ranked chart + why-rendered) → 4×-throttle Scheduler track + `web-vitals` INP baseline (342ms poor) → one fix (`useDeferredValue` to move the table render off the Blocking lane — concurrent-rendering owns the mechanism, this article measures it) → verify the lane moved + INP 180ms good → confirm the Compiler's ✨ half → verify-the-loop table. Deliberately did NOT re-teach the typing-lag recipe's colocation/composition fix (cited it as owner) or the memo/Compiler mechanism (cited 16/24) — this article owns *measurement*, not the fixes. **Raised first-pass self-audit caught one real gap before delivery:** the walkthrough claimed "full files" but omitted the `Product` type — patched. **Inferred/unverified decisions surfaced for review:** (a) cross-link anchors `#render-and-commit`, `#bailouts`, `#usedeferredvalue` are guessed slugs (same carried-over anchor review item — no real target files on disk to verify against); (b) recipe cross-refs kept in "See also" as relative links rather than the frontmatter `related` block, since the real-file `related` shape only showed concept `<folder>/<slug>` entries — did not invent a recipe-prefix convention. **Wave 4 at 7/10 (27–32 + 34); 33 + 35–36 remain. Recipes at 6.** — `server/nextjs-and-rsc-in-practice.md` (440 lines) delivered as article 33 — the second-heaviest forward-debt; gates the ssr-and-rsc recipe track. Next.js 16 (requires React 19.2) App Router caching model verified against official Next.js docs before writing: the load-bearing shift is that **caching is opt-in / dynamic-by-default** — Cache Components = **PPR + `use cache`** (the experimental `experimental.ppr` flag + `experimental_ppr` route config removed), the framework prerenders a static shell and errors ("Uncached data accessed outside `<Suspense>`") if request-time data is neither cached nor streamed; `cacheLife`/`cacheTag` now stable (no `unstable_` prefix); invalidation trio `updateTag` (Server-Action-only, immediate read-your-own-writes) vs `revalidateTag(tag,'max')` (stale-while-revalidate, also Route Handlers; single-arg form deprecated) vs `revalidatePath` (blunt); `params`/`searchParams`/`cookies()`/`headers()` async-only in 16; React Compiler built-in (`reactCompiler:true`, Babel→Rust); Turbopack default; `middleware.ts`→`proxy.ts`. Thesis = Next.js is where the RSC *model* (owned by 26) becomes a production app; the three-tier mental model (static / cached-dynamic via `use cache` / runtime-dynamic via `<Suspense>`) is the spine. Paid ALL owed debts: production RSC (App Router/caching/revalidation), production SSR + partial-prerendering (static-shell-plus-stream), the Next.js double-compile note (framework owns the compiler wiring — you don't wire `@rolldown/plugin-babel` yourself; cited 24), Server Actions in the App Router (server half here; cited 23 for `useActionState`/client half), the framework-mode routing note (cited 28 — App Router is the framework-mode router RR's data-mode article fenced out). Walkthrough (highest-leverage, ~215 lines) = a production product page end-to-end: three tiers on one page (cached `getProduct` + streamed cookie-reading `Recommendations`) → Server Action `submitReview` + `updateTag` read-your-own-writes wired through `<form action>`/`useActionState` → the **RSC→TanStack-Query hydration bridge** (`prefetchQuery` on the server → `<HydrationBoundary state={dehydrate(qc)}>` → warm `useQuery` on the client, no first-paint spinner; cited 27). State-placement spine held across the server boundary (server state → Query cache, never mirrored into a client store). Deliberately did NOT re-teach the RSC model (26), SSR fundamentals (25), the Compiler mechanism (24), the Actions client machinery (23), or the Query cache (27) — cited each owner; this article owns *production wiring*. **Raised first-pass self-audit caught three real gaps before delivery:** `@/lib/*` infra referenced without stating it's an elided stub (fixed with a one-line note so "full files" is honest), "Route Handler" leaned on without a gloss (fixed), and the route-level `loading.tsx`/`error.tsx` conventions absent next to the manual `<Suspense>` (added, connecting the primitive to how real apps wire streaming/errors; cited 17 for the error-boundary mechanics). **Inferred/unverified decisions surfaced for review (carry-over anchor item):** cross-link anchors `#the-payload`, `#vite-wiring`, `#useactionstate`, `#hydration`, `#the-placement-funnel`, `#shared-schema`, `#streaming` are guessed slugs — no real target files on disk to verify against. **Wave 4 at 8/10 (27–34 done); only 35–36 remain. Recipes at 6.** Next up: recipe interleave is due (2 articles since the last recipe) — recommended `state-management/zustand-goes-stale` (the single-most-valuable recipe; server-state-in-a-client-store, article 29's #1 mistake points at it), OR finish the article tail with 35 `tanstack-router` / 36 `accessibility-in-react`. — **Recipe interleaved.** `recipes/state-management/zustand-goes-stale.md` (240 lines) delivered as recipe #7 — the state-management track's cardinal case and the concrete failure behind the entire state-placement spine (article 29's mistake #1 points straight here). Writable since 29 landed; Query v5 / Zustand v5 surface (`staleTime`, `refetchOnWindowFocus`, `invalidateQueries`, `persist`) already in the verified-facts registry and settled, so no re-verify. Scenario = admin inventory dashboard caches fetched stock in Zustand; warehouse sync drops a SKU to 0, operator's tab shows 5-hour-stale counts → oversell/refund/incident. Escaped QA via single-session/single-tab testing + the writer's own write-through masking it (the store reflects the writer's own writes, silently misses every other writer). Staged fix arc: name the category error (a client store lacks every property server state needs — staleness clock, dedup, cancellation, invalidation → those are *cache* properties) → reject three anti-fixes (interval-poll = hand-rolled racing cache, cites `search-race-condition`; refresh button = staleness-as-user's-job; write-through-after-mutation = the exact QA-passing trap) → move server state to TanStack Query (`staleTime` + default `refetchOnWindowFocus` bounds the multi-tab/second-writer staleness that caused the incident + `invalidateQueries` on mutation pulls server truth, reflecting all writers) → keep the client half (selections/filters/sort) in Zustand (the funnel's residue). 5 variations (current-user-still-Query, `persist`-is-a-staleness-trap → Query's `persistQueryClient` with maxAge/buster, optimistic-via-`onMutate` not a store, WebSocket-into-`setQueryData`, RSC→Query hydration bridge — cites 33). 11 pitfalls + a real when-NOT block (a one-time frozen snapshot — signed quote / boot config / captured wizard input — is client state you happened to fetch, not live server state; the test = "would a fresher server value improve correctness?"). Dense cross-ref per the composition-IS-content philosophy (cites 29 funnel + decision-table, 27, context, search-race, context-rerenders sibling, double-submit, 33 bridge). **Raised first-pass self-audit caught one real gap before delivery:** the scenario opened a concrete incident (two writers, 5h staleness, oversell) but the walkthrough never closed the loop — added a verify beat re-running the two-tab repro to confirm focus-refetch convergence, matching recipes 4–6. **Inferred/unverified decisions surfaced for review:** (a) `difficulty: intermediate` — the carried-over difficulty-vocab review item; still unverified against old recipes (no recipe files on disk), assumed `intermediate`/`advanced` from the handoff; (b) recipe `related` block kept to concept `<folder>/<slug>` entries with recipe cross-refs in "See also" — same convention decision as the articles, recipe-`related` shape still unverified; (c) two-level recipe relative-link paths (`../../<folder>/`, `./sibling.md`, `../<track>/`) applied per the handoff spec — unverified against a real placed recipe. **Wave 4 at 8/10 (27–34); 35–36 remain. Recipes at 7** (state-management track now has 2: context-rerenders + zustand-goes-stale). Next up: finish the article tail — 35 `tanstack-router` (type-safe alternative; no hard debts) or 36 `accessibility-in-react` (focus/live-regions/ARIA + Wave-4 debts from testing/styling/forms). ~3 deliveries into this chat. — `ecosystem/accessibility-in-react.md` (369 lines) delivered as article 36. Version-sensitive surfaces verified against current docs before writing: React 19's first-class `inert` **boolean** prop (pre-19 needed a string-attribute workaround), the native `<dialog>` + `showModal()` as the 2026 default (free focus trap + Esc + top-layer + `::backdrop`), `useId` (SSR/portal-safe associations), and the headless-a11y landscape (React Aria / Radix / Headless UI). Thesis = a11y is a discipline React makes easy to skip (a `<div onClick>` ships like a `<button>`); three pillars = semantics-first / focus management / announcements; the through-line is "don't fight the platform." Paid ALL owed debts: focus-trap depth (cited 12's containment embassy), full modal a11y via portals (cited 18), testing's tests-as-a11y-check *in depth* (role queries fail without an accessible role/name + `jest-axe`; cited 31), styling's semantics-orthogonal-to-styling (`className` never touches the a11y tree; cited 32), and forms' `role="alert"`/`aria-invalid`/`aria-describedby` field errors (cited 30/7). Under-the-hood traced the accessibility tree (why a `<div role=button>` is a lie the keyboard doesn't honor), ARIA-in-JSX naming divergence (`aria-*` hyphenated vs `htmlFor`/`tabIndex` camelCased), why `useId` not a counter (hydration; cited 25), and `inert` as the focus-trap primitive. Walkthrough (highest-leverage, ~175 lines) = an accessible modal three ways: the naive broken version (names the 5 WCAG failures) → native `<dialog>` (the default) → custom portal with `role=dialog`/`aria-modal`/`useId` label + focus-in + `inert`-background-trap + focus-restore-on-close + Esc → accessible form errors inside → the production answer (React Aria/Radix + test with a real screen reader). Real-world: route-change focus (cited 28), live regions (polite vs assertive, empty-then-fill), semantics-vs-styling, tests-as-a11y-gate, `:focus-visible`, landmarks/skip-links, reduced-motion. Deliberately did NOT re-teach portals (18), refs (12), the validation flow (30/7), or RTL query mechanics (31) — cited each owner; this article owns *accessible output*. **Raised first-pass self-audit caught one real gap before delivery:** `className="sr-only"` was leaned on for the live region (and implied for skip links) but never defined — added the clip-pattern CSS with the `display:none`-hides-from-AT warning (a wrong impl silently kills the announcement). **Correction made mid-delivery:** initially mis-noted this as "completes the 36-article track" — WRONG, 35 `tanstack-router` is still queued; corrected to 35/36 drafted. **Inferred/unverified decision surfaced for review (carry-over anchor item):** cross-link anchors `#the-containment-embassy`, `#hydration-mismatch`, `#ref-as-a-prop`, `#portals`, `#the-query-priority-ladder`, `#field-errors`, `#semantics` are guessed slugs — no real target files on disk to verify against. **Wave 4 at 9/10 (27–34 + 36); only 35 `tanstack-router` remains. Recipes at 7. Concept track at 35/36 — one article from the entire concept track being drafted.** Next up: **35 `tanstack-router`** closes the concept track (type-safe router alternative; when it beats React Router; no hard forward-debts owed), OR interleave another recipe (backlog still large — `data-fetching/spinner-never-resolves`, the `auth/` and `routing/` and `testing/` tracks all unblocked). ~4 deliveries into this chat; will offer the session-6 handoff around ~10 or when the tail closes. — `ecosystem/tanstack-router.md` (331 lines) delivered as article 35. **🎉 This CLOSES the 36-article concept track — all 36 concept articles (Waves 1–4) are now drafted.** TanStack Router v1 verified against official docs before writing: file-based routing + plugin-generated `routeTree.gen.ts`, `createFileRoute('/path')` with the path literal as the type anchor, the one-time `Register` module augmentation that makes `Link`/`useParams`/`useSearch`/`useLoaderData` inferred, `validateSearch` (Zod schema; `.catch()` silently repairs vs `.default()` → `errorComponent`/`VALIDATE_SEARCH`), `loaderDeps` (search deps that re-trigger the loader), `beforeLoad` guards (`throw redirect`), typed route context, the TanStack Query integration (queryClient-in-context, `ensureQueryData` prefetch in loaders, shared `queryOptions`, `useSuspenseQuery` read), `createRootRouteWithContext`, `getRouteApi` for code-split components, and the `tanstackRouter` Vite plugin (`@tanstack/router-plugin/vite`, must precede `@vitejs/plugin-react`). Thesis = the type-safe alternative; headline capability = **search params as validated, typed state** (the thing RR's stringly-typed `useSearchParams` can't do cleanly). No hard debts owed; paid the one soft ref — 28's "loader-prefetch-into-Query with typed route context." Framed decision-focused (roadmap keeps RR the baseline; two routers in every walkthrough would double examples), with an explicit **when-it-beats-RR / when-it-doesn't** beat (TS-heavy + URL-state-heavy + Query-first → TanStack Router; ecosystem breadth + simple URL state → RR; full-stack → TanStack Start vs Next.js, not RR) and the **honest TS-inference cost** (search-union check scales with route count; the loader-return-inference `await`-don't-return trick). Walkthrough (highest-leverage) = typed search-param filters + a Query loader end-to-end (context+queryClient → Zod `validateSearch` + `loaderDeps` + `ensureQueryData` → typed `<Link search={prev=>...}>` writes + `useSuspenseQuery` read → typed `$productId` detail route), all inferred, no casts. Deliberately contrasted RR (28) rather than re-teaching it; cited 27 (Query), 21 (Suspense/code-split), 30 (Zod), 33 (TanStack Start). Avoided the contested "RR v7 rebranded as Remix" branding claim some sources make — kept the comparison to stable technical axes (type safety, search params), per evenhandedness. **Raised first-pass self-audit caught one real gap before delivery:** every code example depends on the plugin-generated route tree, but the Vite-plugin setup was only described in prose — added the `vite.config.ts` (verified the current `tanstackRouter` named export + `target:'react'`/`autoCodeSplitting` + the enforced plugin-order-before-react gotcha via a quick targeted search). **Inferred/unverified decisions surfaced for review (carry-over anchor item):** cross-link anchors `#usesearchparams`, `#the-placement-funnel`, `#the-decision-table`, `#how-this-evolved` are guessed slugs — no real target files on disk to verify against. **Wave 4 COMPLETE (10/10, 27–36). Concept track COMPLETE — all 36 articles drafted (Waves 1–4). Recipes at 7.** Milestone: the entire concept-article track is drafted; remaining Phase-1 work is the recipe backlog + the review passes (roadmap approval, anchor verification, section-deviation confirmations, difficulty-vocab, `status`-shape already fixed by Huy). Next up: recipe backlog only now — recommended `data-fetching/spinner-never-resolves-after-unmount` (last of the data-fetching symptom trio) or an `auth/`/`routing/`/`testing/` track opener; OR the session-6 handoff (approaching ~5 deliveries this chat, and the article tail is now closed — a natural handoff point). ~5 deliveries into this chat. — **Recipe interleaved.** `recipes/data-fetching/spinner-never-resolves-after-unmount.md` (229 lines) delivered as recipe #8 — **completes the data-fetching symptom trio (ordering #1 / dedup #5 / abandonment #8).** Writable since article 9; effects + Query v5 surface settled, no re-verify. Scenario = tabbed dashboard hoists per-tab `{loading,data}` into a store; throttled-phone fast tab-switch mid-fetch aborts the request (correct cleanup) but the `ignore`-guarded reset is skipped → `loading` stuck `true` → remount's `if(loading)return` guard skips the refetch → permanent spinner; ~1-in-20 mobile, escaped QA via ~4ms localhost fetch (abort branch never runs in dev). Staged fix arc: name it (a `true` that never became `false` — request-derived state stored as a hoisted flag whose reset path an abandoned request skips; "a flag you set can strand, a value you derive cannot") → reject two naive fixes (cleanup-reset flashes in StrictMode remount + still allows impossible states; drop-the-guard reintroduces the stale-response race → cites search-race) → the real fix: derive loading from the request via TanStack Query (`status`/`isPending` derived from live query state can't strand; `queryFn`'s `{signal}` cancels; data survives the switch via cache; store shrinks to "which tab" — cites zustand-goes-stale + the spine) → hand-rolled fallback: a **local discriminated-union** `RequestState` (destroyed with the component, restarts at `idle` on remount, spinner derived from the union). 3 variations (Suspense promise-must-settle-on-abort → cites suspense; global in-flight counter decrement-in-`finally`; route-loaders cancel-on-nav → cites routing). 11 pitfalls + a real when-NOT (fire-and-forget writes → `sendBeacon`, no loading to track; structurally-can't-unmount-mid-request → safe pattern still cheap). Verify-the-loop built in from the start (throttle + fast tab-switch repro → resolves after fix). Also folded in the React-18-removed-the-unmounted-setState-warning fact (the abort-less path is now a silent stale-data bug, not a warning; cites strictmode-double-mount). Raised first-pass self-audit found no genuine gaps (infra stubs conventional, verify beat + cross-refs + when-NOT all present) — no patch needed this delivery. **Inferred/unverified decisions surfaced for review:** `difficulty: intermediate` again (carried-over difficulty-vocab item, consistent with recipe 7's assumption); recipe `related` kept to concept `<folder>/<slug>` with recipe cross-refs in "See also"; two-level relative-link paths per the handoff spec — all still unverified against a placed recipe. Anchor cross-links (`#the-abortcontroller-pattern`) guessed. **Wave 4 COMPLETE (10/10). Concept track COMPLETE (36/36 drafted). Recipes at 8** (data-fetching track: 3 landed + trio complete; state-management: 2; performance/forms-and-ux: 1 each). Next up: recipe backlog continues (auth/ refresh-storm, routing/ back-button-scroll, testing/ flaky-waitFor track openers all unblocked) OR the session-6 handoff — **the article tail is closed and we're ~6 deliveries in, so a handoff is now the natural next move.** — **Recipe interleaved (auth track opened).** `recipes/auth/refresh-storm-on-401.md` (~185 lines) delivered as recipe #9 — opens the auth recipe track (roadmap track 5, "every real app"). Writable since 27+28; a concurrency pattern over settled fetch/Query/Router primitives + stable token-storage security consensus, so no version re-verify. Scenario = 6 parallel widget loads on a refocused idle tab → 6 simultaneous 401s → 6 concurrent `/auth/refresh` calls; refresh-token **rotation** makes the first succeed and the other 5 present a consumed token → rejected → app logs the user out despite a valid session (~1-in-3 post-idle loads). Escaped QA: sequential test requests never collide + rotation often staging/prod-only + needs expired-token-at-multi-request-load. Staged fix arc: name it (refresh is per-request, should be per-app; rotation turns the race destructive) → reject the naive `isRefreshing` boolean (stops extra refreshes but drops the requests that should wait+retry — a flag isn't a queue) → **single-flight refresh with a shared promise** (`refreshPromise ??= refreshToken().finally(()=>refreshPromise=null)` — one refresh, N awaiters, each retries its own request once; the reset lets a later expiry refresh again) → fail-once + integrate (idempotent `onAuthFailure` = `queryClient.clear()` + redirect ONCE for the whole failed batch; authFetch as transport under Query/Router shown in a `queryFn`; Query `retry:false` on 401 so its retry doesn't 3× the storm). Framed as the auth-track equivalent of search-race (dedup concurrent in-flight work — for refresh instead of reads). 4 variations (header-token-in-memory, axios-interceptor queue+replay, proactive-refresh-before-expiry still needs single-flight, mutations-mid-refresh → idempotency care, cites strictmode write classification). 11 pitfalls + a real when-NOT (opaque server-side sessions → none of this applies; single-request pages can't storm but cheap insurance). Security stance: httpOnly+Secure+SameSite cookie for the refresh token / access token in memory / never localStorage / CSRF note; OWASP Session Management referenced. Verify-the-loop: 6 parallel calls → assert exactly 1 refresh + 0 logouts. **Raised first-pass self-audit caught one gap:** Stage 4 asserted "wire authFetch under everything" without showing it — added a concrete `queryFn: () => authFetch(...)` snippet so the integration seam is shown, not claimed. Line count (~185) is on the shorter side but the single-flight surface is genuinely small; audited as coverage-complete (padding to match the 200–240 recipes would violate decision-not-tour / coverage-bar). **Inferred/unverified surfaced for review:** `difficulty: advanced` (this recipe genuinely warrants it — rotation + concurrency + interceptors; carried-over difficulty-vocab item, consistent with the assumed intermediate/advanced vocab); recipe `related` = concept slugs + recipe refs in See also; two-level relative paths per handoff; guessed anchor `#read-vs-write` (into strictmode-double-mount). **Recipes at 9** (data-fetching 3 [trio complete] / state-management 2 / performance 1 / forms-and-ux 1 / auth 1 [track opened]). Wave 4 + concept track both COMPLETE (36/36 articles). Next up: continue the recipe backlog (routing/ or testing/ track openers, or the remaining auth pair flash-of-protected-content / logout-leaves-stale-cache) OR — recommended — the **session-6 handoff**, now clearly overdue: the article tail is closed, we're ~7 deliveries in, and a fresh chat should carry the completed-concept-track state + the still-open review items (anchors, difficulty vocab, recipe `related` shape, section deviations) forward cleanly. — **Recipe interleaved (routing track opened).** `recipes/routing/params-out-of-sync.md` (161 lines) delivered as recipe #10 — opens the routing recipe track (roadmap track 6). Writable since 28; settled `useSearchParams`/`validateSearch` surface, no re-verify. **Pays the forward-ref I created in article 35's See-also** ("a failure typed search params largely design out"). Scenario = product listing keeps category+page in the URL AND mirrors them into `useState` seeded on mount; Back after clicking into a product restores the URL but not the un-remounted component's state → grid drifts stale; "Clear filters" and sibling-param-wipe compound it. Escaped QA: initializer reads on mount so navigate-to-URL-and-assert tests pass; bug needs URL-change-without-remount (Back/Forward, query-only Link, programmatic navigate). Staged fix arc: name it (the URL is already durable/shared/history-aware state; mirroring makes an unsyncable second copy) → reject the `useEffect` URL→state sync (the derive-don't-mirror anti-pattern; cites 14 + you-might-not-need-an-effect) → delete the `useState`, derive during render from `params`, write via functional `setSearchParams(prev=>…, {replace:true})` (preserves siblings, no history spam) → parse/validate on read, where TanStack Router `validateSearch` designs it out (cites 35). Framed as derive-don't-mirror applied to the address bar. 4 variations (local-input-buffer + debounce the committed URL write, preserve-vs-reset siblings, push-vs-replace, TanStack Router typed search). 10 pitfalls + a real when-NOT (ephemeral/non-shareable UI state stays local — the funnel decides; forcing it into the URL is the opposite mistake). Verify-the-loop: filter→page3→product→Back → grid matches URL. **Raised first-pass self-audit caught a real correctness bug before delivery:** the scenario's opening line was a broken/nonsensical `const [searchParams,setSearchParams]=useState;` stub with a duplicate `useSearchParams()` below — fixed to the correct single `const [params,setSearchParams]=useSearchParams()`. (This is the highest-value kind of audit catch — a shipping code error, not just a coverage gap.) Line count 161 on the shorter side but the URL-as-source-of-truth topic is focused; audited as coverage-complete, not padded. **Inferred/unverified surfaced for review:** `difficulty: intermediate`; recipe `related` = concept slugs + recipe refs in See also; two-level relative paths; guessed anchors (`#derive-vs-mirror`, `#usesearchparams`, `#search-param-validation`, `#the-placement-funnel`). **Recipes at 10** (data-fetching 3 [trio complete] / state-management 2 / performance 1 / forms-and-ux 1 / auth 1 [opened] / routing 1 [opened]). Wave 4 + concept track both COMPLETE (36/36). ✅ `recipes/index.md` longer-form index TODO is now unblocked (≥10 recipes exist). Next up: **session-6 handoff strongly recommended** — 8 deliveries this chat, article tail long closed, 4 recipe tracks now have openers; a fresh chat should inherit the completed-track state + open review items. Otherwise continue recipes (testing/ opener, or remaining auth/routing pairs). — **Session 5 closed.** `handoff-session-6.md` created (full ownership map: all 36 concept articles + 10 recipes; the recipe backlog with forward-ref-paying priorities; the session-5 version-facts additions; the resolved `status`-shape; and the carried review items — prominently the anchor-verification pass across 27–36 + recipes 5–10, plus difficulty vocab, recipe-`related` shape, and the section deviations). **Final session-5 state: Waves 1–4 COMPLETE — all 36 concept articles drafted; 10 recipes across 6 tracks (data-fetching 4 / state-management 2 / performance 1 / forms-and-ux 1 / auth 1 / routing 1); testing/real-time/ssr-and-rsc/pwa-offline tracks unblocked but not yet opened.** Session-5 deliveries this chat: articles 34, 33, 36, 35 (closing the concept track) + recipes 7, 8, 9, 10 (zustand-goes-stale, spinner-never-resolves completing the data-fetching trio, refresh-storm opening auth, params-out-of-sync opening routing) = 8 deliveries. Next session opens with the recipe backlog — recommended `auth/logout-leaves-stale-cache` (pays recipe #9's forward-ref) — or clears the carried review items first.- **Session 6 (July 2026) — review passes (no new recipes/articles).** Huy supplied the live GitHub repo, unblocking the file-dependent review passes. **Anchor verification DONE:** cloned, GitHub-slugified every heading, checked all 90 anchored cross-links → **all 83 broken anchors resolved** and delivered as `anchor-fixes.patch` (+ `session-6-review-findings.md`). Most were in-place corrections (exact-slug + repoint-to-nearest-section); two heavily-referenced ones were made *precise* by small structural improvements Huy greenlit: (1) added `### The placement funnel` to `state-management-landscape.md` — the project's spine concept, 6 inbound refs whose `#the-placement-funnel` anchors were correct as authored, the heading was simply missing; (2) promoted `error-boundaries.md`'s bold-lead sub-blocks (The unwind / concurrent retry / commit-phase throws / React 19 root-level reporting / why-a-class / granularity ladder / reset design / try-catch redirect / stale-chunk / fallbacks-stay-dumb) to `###` headings — this resolved five inbound anchors precisely (`#the-unwind`, `#placement-the-granularity-ladder`, `#reset-design`, `#react-19s-root-level-reporting`, `#the-trycatch-redirect-paid`) and brought that lone flat article into corpus heading-consistency. **Correction logged:** an earlier claim that error-boundaries lacked React-19 root-error-options content was a grep false-negative — the `**React 19's root-level reporting.**` block (onUncaughtError/onCaughtError/onRecoverableError via `createRoot`) was already there; no content added, only the heading promotion. The ~20 remaining repoints (`#semantics`/`#usesearchparams`/`#the-query-priority-ladder`/etc.) point at nearest section by design. ⚠️ **Hard bug for Huy (a `git mv`, in the Cursor prompt): `useref-and-the-dom.md` is in `rendering/` but its own `concept_folder`, progress.md, and all ~19 inbound links say `effects/` → `git mv rendering/useref-and-the-dom.md effects/useref-and-the-dom.md` fixes all 19.** **Recipe frontmatter RESOLVED:** `difficulty` vocab = foundational/intermediate/advanced; recipe `related` carries sibling recipes prefixed `recipes/` — #5/#6 prefix fixed in patch, #7–#9 siblings re-added via the Cursor prompt (#10 has none). **Section-deviation RESOLVED:** "Minimal shapes" (29 & 32) kept as a documented decision-table micro-convention. **Deliverables:** `anchor-fixes.patch` (18 content files incl. the funnel + error-boundaries restructure + progress.md), `session-6-review-findings.md`, `cursor-apply-session6-fixes.md`. **Still open:** roadmap approval; `LICENSE`; getting-started/typescript-for-react onboarding docs; demo hosting; model allocation; `recipes/index.md` (now writable). Next up: recipe backlog — recommended `auth/logout-leaves-stale-cache` (pays recipe #9's forward-ref), then routing pair, then `ssr-and-rsc/` (pays 33) + `performance/` hub recipes (pays 34).
- **Session 6 — anchor precision round 2.** After Huy greenlit making the remaining repoint anchors precise "where there's a real block," revisited the ~15 nearest-section repoints. **4 promoted to real `###` headings** (their concepts were already coherent bold-lead blocks): `testing`→`### The query priority ladder`, `useref-and-the-dom`→`### The containment embassy`, `styling-approaches`→`### Styling is orthogonal to the Compiler and to a11y`, `concurrent-rendering`→`### Async transitions`; inbound anchors updated. **3 precision-repointed** (no clean block, but a more accurate section exists): routing `useSearchParams`→`#api--type-reference`, Query hydration note→`#how-this-evolved`, and react-compiler's `use-no-memo` link→ the real owner `foundations/rules-of-react.md#use-no-memo--the-labeled-exception`. **The rest left as section-level repoints** (woven into prose or numbered lists — `#purity` is rule #1 in a list, `#render-and-commit` spans two headings, `#hooks-as-a-linked-list` is a bullet — manufacturing headings there would be artificial). **Result: every real anchored cross-link in the project now resolves** (verified; the only non-resolving anchor is the illustrative `../path/to/file.md#section-anchor` example inside this file). Delivered as `anchor-fixes-2.patch` (diffs against the patch-1+mv state).
- **Session 6 — recipe interleaved (auth teardown).** `recipes/auth/logout-leaves-stale-cache.md` (168 lines) delivered as recipe #11. First recipe written with **verified anchors** (repo on disk now) — all outbound cross-links checked to resolve against real headings, no guesses. **Pays recipe #9's forward-ref:** the shared cache-clear-on-logout teardown that #9's `onAuthFailure` must reuse; completes the auth teardown story (opener #9 + teardown #11; only `flash-of-protected-content` remains in the auth track). Scenario = shared-workstation cross-user cache leak (root `QueryClient` outlives a client-side logout `navigate`, so agent B sees agent A's cached PII; worse with a persister on disk). Fix arc: name it (logout is a teardown; token=credential, cache=the data it unlocked, both correctly-placed server state → incomplete teardown, not misplacement — cites the placement funnel) → `queryClient.clear()` not invalidate/reset, `cancelQueries` first + right order → clear the persisted copy (`removeClient`/`buster: userId`, or don't persist authed data / `gcTime:0`) → cross-tab `BroadcastChannel` + one shared `endSession` with the 401 path (cites #9 stage 4). 5 variations, 12 pitfalls, real when-NOT (single-identity device keeps cache as warm-start). Verify-the-loop closes against the incident (A→logout→B → empty cache + no A-keyed localStorage + tabs redirected). Cites 27 (Query/`clear`), 28 (protected-route redirect), zustand-goes-stale (residue client state), the funnel; security stance consistent with #9 (in-memory access token / httpOnly refresh cookie / `POST /logout`). **Raised first-pass self-audit caught one accuracy bug:** prose claimed `clear()` "cancels and removes every query" — but `clear()` doesn't abort in-flight requests (that's the separate `cancelQueries()` step, and the whole reason it runs first); corrected. **Inferred/surfaced for review:** `difficulty: intermediate` (core insight graspable; persistence/multi-tab/server-session are additive hardening layers — could argue `advanced`, chose intermediate); `related` uses the confirmed `recipes/`-prefixed sibling convention (data-fetching-tanstack-query + routing-react-router + state-management-landscape concepts, refresh-storm + zustand-goes-stale recipes); line count 168 is on the shorter side but the teardown surface is focused — audited coverage-complete, not padded. **Recipes at 11** (data-fetching 3 / state-management 2 / performance 1 / forms-and-ux 1 / auth 2 / routing 1). Next up: `auth/flash-of-protected-content` (completes the auth track) or the routing pair (`back-button-scroll`, `lazy-route-flashes-blank`) or open `ssr-and-rsc/` (pays 33's See-also debts) / `performance/` hub recipes (pays 34's).
- **Session 6 — recipe interleaved (auth guard, track closed).** `recipes/auth/flash-of-protected-content.md` (145 lines) delivered as recipe #12 — **completes the auth trilogy (opener #9 refresh-storm + teardown #11 logout + guard #12) → the auth recipe track is done.** Verified anchors (repo on disk). **Pays recipe #9's See-also forward-ref.** Scenario = cold start on a protected URL (hard reload / bookmark / deep link) with the access token wiped + a silent refresh in flight, so first paint has `user==null`; a binary guard either flashes protected PII before redirecting or wrongly redirects an authed user to `/login` then bounces back. Escaped QA: dev `/me` ~2ms (loading window = 1 frame) + forward-only client-side nav never hits the `loading` state. Fix arc: name it (auth is 3 states, not `user|null` — collapsing `loading` into a decided branch IS the flash; cites thinking-in-react `#model-states-not-booleans`) → SPA `RequireAuth` gates on the discriminated union (redirect in render via `<Navigate>`, NOT `useEffect`) → **flash-free-by-construction in RR data mode: `throw redirect` in a parent-layout loader so the protected component never mounts** (cites 28) → bootstrap-once + return-trip (`?next`/`from`; httpOnly-cookie SSR → zero client `loading` + no hydration mismatch, cites 25). 5 variations (component-vs-loader, preserve-destination, SSR-zero-loading, authorized≠authenticated=403-not-login, refresh-in-progress-awaits-#9's-single-flight). 12 pitfalls + real when-NOT (public routes / SSR-always-decides-first → guard redundant; test = "can first paint precede knowing auth?"). Verify-the-loop closes 3 cold-start paths. Cites 28 (loaders/redirect), 25 (hydration), thinking-in-react (state modeling), #9 (single-flight refresh), #11 (teardown side). Raised first-pass self-audit found no shipping bug (code + verify-loop + when-NOT + cross-refs all present) — no patch needed. **Inferred/surfaced:** `difficulty: intermediate`; `related` = routing-react-router + data-fetching-tanstack-query + ssr-and-hydration concepts, refresh-storm + logout recipe siblings (`recipes/`-prefixed); 145 lines short but the guard surface is focused — audited coverage-complete, not padded. **Recipes at 12** (data-fetching 3 / state-management 2 / performance 1 / forms-and-ux 1 / **auth 3 [track COMPLETE]** / routing 1). Next up: routing pair (`back-button-scroll`, `lazy-route-flashes-blank` — #10 references both) to close routing; or open `ssr-and-rsc/` (pays 33's 3 See-also debts) / `performance/` hub recipes (pays 34's) / `testing/` opener (gated by 31).
- **Session 6 — Wave 5 opened (micro-frontends scope added + article 37 drafted).** Huy approved a real roadmap extension: **2 concept articles (new `architecture/` folder, Wave 5) + recipe track #11 `micro-frontends/`.** Scope added to `roadmap.md` (§3 Wave 5 block, §4 track-11 row, §5 conventions line) and `progress.md` (this Phase 1d section). MF tooling verified via web search (July 2026): on **Vite 8/Rolldown, Module Federation is not native yet** → `@module-federation/vite` (wraps `@module-federation/runtime`) / `vite-plugin-federation` 1.0 (manifest-first, SSR-aware) / older `@originjs/vite-plugin-federation`; **MF 2.0/3.0** = runtime + manifest-first (`mf-manifest.json`) + shared singletons + DTS type-sharing; Rspack first-class; **Rolldown-native MF landing**; alternatives = Native ESM/import-maps, single-spa, **Next.js Multi-Zones** (routing layer), Server-Side MF/RSC-across-origins (2025); honest consensus = *organizational not technical, ~4+ FE teams threshold, most teams regret early adoption* (`@module-federation/enhanced` is the webpack/Rspack package name). **Structural decision vs the angular-concepts MFE article (Huy compared):** Angular's is one article, walkthrough-first + migration-framed (webpack-MF Path A vs Native-Federation Path B dual code) because the Angular CLI *settled* the choice (esbuild → Native Federation or nothing); React's is split **37 decision / 38 practice**, greenfield (no dual code), because React's composition choice is genuinely open (MF vs Multi-Zones vs Server-MF vs monorepo vs web components — a live meta-framework axis Angular lacks). Ported the *depth* of the Angular mechanism-reflection into 38's plan (runtime-share-scope vs native-ESM-import-map), not the structure. — **`architecture/micro-frontends.md` (224 lines) delivered as article 37 — the decision anchor.** Decision-not-tour, when-NOT-led. Owns: MFE-as-org-pattern (+ 4 "it is NOT" clarifications: not perf, not a monolith upgrade, not microservices, not code-org); the composition-style taxonomy traced by *where composition happens* (build-time/monorepo · browser-runtime MF/import-maps · server/edge · routing-layer/Multi-Zones · web components · iframes); the decision funnel (5 ordered questions, stop at first yes) + decision table + org-scale threshold; shared-kernel + integration-contract; cross-MFE shared state as the cardinal anti-pattern (cites 29's placement funnel across the boundary); version-skew governance (N·(N−1)/2); failure isolation (cites 17's `#placement-the-granularity-ladder` + `#the-stale-chunk-fallback` — a dead remote entry *is* a failed chunk); styling isolation (cites 32); the SSR/RSC intersection (cites 26/33 — SSR pushes off client federation toward server/edge). 10 mistakes (lead: "architecture without autonomy is cosplay"), how-this-evolved, 3 decision exercises. **Minimal shapes** rename applied (decision-table convention, per 29/32). **Self-audit:** anchors verified against real files (all resolve); the 5 `./module-federation.md` refs are forward-refs to 38 (unanchored, resolve when it lands) — no shipping bug found. **Inferred/surfaced:** article `related` includes the sibling `architecture/module-federation` (folder-prefixed, forward); 224 lines is right for a decision article (cf. 29=279, 32=237); zero-Angular framing of the ESM/import-map approach. **Next up: article 38 `architecture/module-federation`** — the practice + the deep substrate trace (share-scope vs import-map), MF 2.0 on Vite 8. Then the `micro-frontends/` recipe track. **Concept track now 37/38 drafted (Wave 5); recipes still 12; SSR/RSC recipes parked per Huy.**
- **Session 6 — article 38 drafted; Wave 5 concept articles COMPLETE.** `architecture/module-federation.md` (337 lines) delivered as article 38 — the practice, closing the Wave-5 concept pair (37 decision + 38 practice; the `micro-frontends/` recipe track remains). MF 2.0 / `@module-federation/vite` surface used (verified this session): remote `federation({ name, filename, manifest:true, exposes, shared, dts.generateTypes })` + `server.origin`; host `remotes:{ checkout:{ type:'module', name, entry:'…/mf-manifest.json' } }` + `shared` + `dts.consumeTypes`; runtime API `registerRemotes`/`loadRemote`; `shared` sub-options `singleton`/`strictVersion`/`requiredVersion`/`eager`; the no-`manualChunks` gotcha; remotes-must-be-built asymmetry. **Owns:** the *how* — and the crown-jewel under-the-hood traces BOTH substrates (bundler-runtime + share-scope `loadShare` interception vs native-ESM + import-map module-record identity), showing config options + `Invalid hook call` + the async boundary all map back to one of them — the depth parity Huy asked for vs the angular mechanism reflection, React-flavored, zero Angular. Walkthrough = shell+checkout end-to-end (typed `@mf-types` exposes/consume → singleton proof via `shared:[]`→`Invalid hook call` → per-remote error boundary as the stale-chunk-fallback pattern → dynamic `registerRemotes` + resilience policy → Vite plugin wiring gotchas). Cites 37 (decision `#the-decision`/governance/anti-pattern), 17 (`#the-stale-chunk-fallback` + `#placement-the-granularity-ladder`), 29 (`#the-placement-funnel`), 24 (`#the-vite-8-wiring-and-the-gotcha`), 25 (`#real-world-patterns`). 10 mistakes, API/type reference table, how-this-evolved (webpack MF → MF 2.0 runtime/manifest/DTS → native-ESM/import-maps → Server-MF), 3 exercises. **Raised first-pass self-audit caught one shipping bug pre-delivery:** a self-link `#failure-isolation-and-resilient-loading` didn't match the actual heading slug `#step-4--failure-isolation-and-resilient-loading` — fixed (the anchor-verify pass, now runnable against the real repo, caught it mechanically). All 137 anchored cross-links in the project resolve (only the illustrative `../path/to/file.md` example line remains); article 37's 5 forward-refs to 38 now resolve. **Inferred/surfaced:** article `related` cross-links the sibling 37; `@module-federation/vite` chosen as the primary Vite plugin (most-referenced official-ish; noted Rolldown-native is landing + "verify version against the registry"); 337 lines is practice-article range (cf. 27=428, 33=440, 35=331). **Wave 5 concept articles COMPLETE (37+38 drafted). Concept track now 38 articles total (36 core + 2 architecture). Recipes still 12.** Next up: **open the `micro-frontends/` recipe track** (track #11) — recommended opener `shared-react-duplicated` (the `Invalid hook call` singleton-misconfig bug, the track's cardinal case, forward-ref'd by both 37 and 38), then `remote-failure-blanks-shell` / `version-skew-breaks-host`. SSR/RSC recipes still parked per Huy.
- **Session 6 — micro-frontends recipe track OPENED.** `recipes/micro-frontends/shared-react-duplicated.md` (128 lines) delivered as recipe #13 — opens track #11, the last new track from the Wave-5 scope, and **pays the singleton forward-refs from both article 37 and 38.** New `recipes/micro-frontends/` folder. `difficulty: advanced` (federation + version negotiation + transitive-dep + dev/prod-divergence diagnosis — genuinely advanced context). Scenario = the cardinal MFE bug: a `checkout` remote flawless standalone + composed-for-months, a routine React patch bump ships `Invalid hook call` to every `/checkout` user; cause = `shared` without `singleton:true` → matching versions masked it, the bump forked React into two module-level dispatchers. Escaped QA via standalone-green + composition-only + latent-until-a-bump ("we didn't change anything"). Fix arc: name it (two Reacts one dispatcher, NOT rules-of-hooks — cites 10; the doubled runtime — cites 38's two substrates) + a concrete diagnostic (log `react` identity both sides / MF shared graph) → reject the error's own first-two-suggestions + `resolve.dedupe` (wrong layer) → `singleton:true`+`requiredVersion` both sides → share the whole runtime kernel (react-dom/router/error-boundary — react-only relocates the crash) + `strictVersion` reject-vs-warn + CI guard (>1 react fails the build). 5 variations, 12 pitfalls, real when-NOT (not-federating→`resolve.dedupe`; deliberately-isolated iframe/web-component→two Reacts intentional). Verify-the-loop: prod `build && preview`+re-bump→one React. Cites 38 (`#the-shared-contract-in-depth`/`#how-it-works-under-the-hood`), 37 (`#shared-dependency-governance-and-version-skew`), 17 (`#placement-the-granularity-ladder`), 10 (`#how-it-works-under-the-hood` = not-a-rules-of-hooks-bug). **Raised first-pass self-audit:** no shipping bug, but flagged the highest-leverage section (walkthrough) was code-light for a reader whose immediate need is *confirming* it's two-Reacts-not-the-other-two-causes → added the concrete identity-check diagnostic snippet to Stage 1 (real coverage gap, not padding). Line count 128 is the shortest recipe yet but the fix surface is one config flag; audited coverage-complete (scenario+escaped-QA / 4-stage arc / 5 variations / 12 pitfalls / when-NOT / verify-loop all present), padding would violate coverage-bar. All anchors verified against real files. **Inferred/surfaced:** `difficulty: advanced` (vs the intermediate auth/routing recipes — federation warrants it); recipe `related` = concept slugs only (first in track, no recipe siblings yet; two planned siblings referenced in See-also with *(planned)*); folder `recipes/micro-frontends/` created. **Recipes at 13** (data-fetching 3 / state-management 2 / performance 1 / forms-and-ux 1 / auth 3 [complete] / routing 1 / **micro-frontends 1 [opened]**). Wave 5 concept articles complete (37+38); the `micro-frontends/` track is opened. Next up: continue the micro-frontends track (`remote-failure-blanks-shell` — isolation/circuit-breaker; or `version-skew-breaks-host`), OR return to the earlier backlog (routing pair `back-button-scroll`/`lazy-route-flashes-blank`; testing/real-time openers). SSR/RSC recipes still parked per Huy.
- **Session 6 — micro-frontends recipe #14 (isolation pillar).** `recipes/micro-frontends/remote-failure-blanks-shell.md` (175 lines) delivered as recipe #14 — the isolation counterpart to #13's singleton fix; **pays the failure-isolation forward-refs from both 37 (`#failure-isolation-is-not-free`) and 38 (`#step-4`).** `difficulty: advanced`. Scenario = a 4-remote shell where one remote's `mf-manifest.json` 404s for 3 min → whole-shell white screen (failed `React.lazy` import throws past a missing/root boundary → tree unmounts); blast radius inverted (least-critical remote blanks the product). Escaped QA: remotes always up in dev/staging → failure path untested. Fix arc: name it (a remote is a runtime fetch that will fail; isolation is built not given) → **per-remote** boundary+Suspense (not root; failed remote = failed chunk, cites 17's stale-chunk-fallback/granularity-ladder/reset-design/fallbacks-stay-dumb; `resetErrorBoundary` manual retry) → resilient loader around runtime `loadRemote` (`Promise.race` timeout + jittered backoff retry; `React.lazy` can't express these) → circuit breaker (threshold+cooldown) + criticality tiers (hide/degrade/full-page = a product decision per remote) + observability. 5 variations, 12 pitfalls, real when-NOT (single-remote-IS-product → full-page correct; build-time-composed → can't fail at load). Verify-the-loop: 404→per-slot fallback / hang→timeout / 3×fail→breaker opens / restore→retry recovers. Real code included (`loadRemoteResilient` + circuit-breaker map + `Remote` wrapper). Cites 38 (`#step-4`/`#placement-the-granularity-ladder`), 37 (`#failure-isolation-is-not-free--you-build-it`), 17 (four anchors), 21 suspense (`#suspending-is-a-throw-catching-is-a-boundary`/`#real-world-patterns`), 28 routing (`#real-world-patterns`), sibling #13. **Raised first-pass self-audit caught one quality bug:** the route-loader variation's link text "React Router data mode" pointed at a *module-federation* section (mislabel) → repointed to `routing-react-router#real-world-patterns` (the actual owner). No shipping code bug. All anchors verified (158 checked, only the progress.md example unresolved). **Inferred/surfaced:** `difficulty: advanced`; `related` adds `concurrent/suspense` + the `recipes/micro-frontends/shared-react-duplicated` sibling (first recipe cross-ref in this track's `related`, `recipes/`-prefixed per convention); 175 lines (comfortable recipe range — more code than #13's config-flag fix). **Recipes at 14** (data-fetching 3 / state-management 2 / performance 1 / forms-and-ux 1 / auth 3 [complete] / routing 1 / **micro-frontends 2**). Next up: finish the micro-frontends track (`version-skew-breaks-host` — the governance pillar, completing the singleton/isolation/governance trio; or `css-leaks-across-remotes` / `cross-remote-state-coupling`), OR return to the earlier backlog (routing pair, testing/real-time openers). SSR/RSC recipes still parked per Huy.
- **Session 6 — micro-frontends recipe #15 (governance pillar); MFE core trio COMPLETE.** `recipes/micro-frontends/version-skew-breaks-host.md` (154 lines) delivered as recipe #15 — the governance counterpart, completing the **singleton (#13) / isolation (#14) / governance (#15)** trio. `difficulty: advanced`. Scenario = a remote's independent deploy silently breaks the host: checkout renames an exposed prop, ships green on its own pipeline, host (not rebuilt, typed against the *previous* `@mf-types`) passes stale props → users pay-and-land-nowhere. Core insight = the exposed surface is a public API across a deploy boundary + type-sharing is a *build-time snapshot* (the runtime remote drifts); "both green, combination broken." Distinct from #13 (shared-*dependency* React-singleton crash) and #14 (remote fails to *load*): here the remote loads fine but is *incompatible*. Fix arc: name it (versioned public API; N·(N−1)/2 — cites 37 governance) → reject trust-types / rebuild-host-every-change (re-couples, cites 37 decision) / pin-everything → version the contract + **runtime** semver gate (refuse+fallback on major mismatch, cites #14) → **evolve additively** (deprecated-alias window, minor-not-break) + contract test in CI (cites 31). 5 variations (criticality-pinning, consumer-driven/Pact, runtime feature detection, Zod-on-the-wire, deprecation telemetry). 12 pitfalls + real when-NOT (pinned+together→no skew→reconsider federation; single-consumer-same-team→shared package simpler). Verify-the-loop: breaking-major→gate fallback / additive→un-rebuilt host works / remove-in-use→CI contract test fails. Real code (`CONTRACT_VERSION`, `satisfies` gate, deprecated-alias widget). Cites 37 (`#shared-dependency-governance-and-version-skew`/`#the-decision`), 38 (`#step-2--the-host-consumes-it-with-real-types`), 31 (`#real-world-patterns`), siblings #13/#14. Raised first-pass self-audit found no shipping bug (code + verify-loop + when-NOT + cross-refs all present); `import { satisfies } from "semver"` valid. **Inferred/surfaced:** `primary_concept: architecture/module-federation` for track consistency (the governance *concept* is 37's, but the fix is MF manifest/versioning/runtime practice — noted the alt); `difficulty: advanced`; `related` = both concept articles + testing + both sibling recipes (`recipes/`-prefixed); 154 lines (recipe range). **Recipes at 15** (data-fetching 3 / state-management 2 / performance 1 / forms-and-ux 1 / auth 3 [complete] / routing 1 / **micro-frontends 3 [core trio complete]**). **Wave 5 fully realized: 2 concept articles (37+38) + the micro-frontends recipe trio.** Two long-tail MFE candidates remain (`css-leaks-across-remotes`, `cross-remote-state-coupling`). Next up: those long-tail MFE recipes, OR return to the earlier backlog — routing pair (`back-button-scroll`/`lazy-route-flashes-blank`, both ref'd by #10), testing/ or real-time/ openers. SSR/RSC recipes still parked per Huy.
- **Session 6 — micro-frontends recipe #16 (styling isolation).** `recipes/micro-frontends/css-leaks-across-remotes.md` (123 lines) delivered as recipe #16. **`primary_concept: ecosystem/styling-approaches`** (32 owns the fix — first MFE recipe pointing primary at a non-architecture article; the styling-isolation lens); **`difficulty: intermediate`** (CSS scoping is a deliberate step down from the advanced federation-internals recipes #13–15). Scenario = catalog's global CSS (reset + `button{}` + `.card{}`) restyles a standalone-perfect checkout when composed — **load-order-dependent** (who wins = which stylesheet loaded later = which remote lazy-loaded first = what route you visited first), so intermittent/path-dependent. Escaped QA: standalone = only CSS on page; collision composition-only + order-dependent; culprits (resets/element-selectors/generic names) look fine isolated. Fix arc: name it (CSS has no runtime module boundary; one document, one global namespace — cites 37 `#styling-isolation`) → reject BEM-convention / `!important` / load-order-coord → scope by construction (hash class names via CSS Modules — cites 32 `#css-modules`; ban global/element/reset selectors in remotes; shared tokens via the kernel — cites 37 `#the-shared-kernel-and-the-integration-contract`) → deterministic precedence via `@layer` (shell declares order once → load-order stops mattering) + Shadow DOM for hard isolation (custom-properties pierce → theming survives; cites 32 `#real-world-patterns`) + top-layer `<dialog>` for cross-remote z-index (cites 36 `#step-2--the-native-dialog-your-default`). 5 variations (CSS-Modules, `@layer`, Shadow DOM, shared-design-system-via-kernel, Tailwind-Preflight-collision → cites 32 `#tailwind`). 12 pitfalls + real when-NOT (one-design-system-through-kernel → hashing suffices; build-time-composed → single build already scopes). Verify-the-loop: both nav orders identical / reset doesn't shift / shared-token change updates both. Real code (CSS Modules hashing, `@layer` order declaration, Shadow-DOM custom-property piercing). Raised first-pass self-audit found no shipping bug (code correct, verify-loop + when-NOT + cross-refs present). **Inferred/surfaced:** `primary_concept` = styling not architecture (the fix's owner; the *track* is still micro-frontends); `difficulty: intermediate` vs the advanced trio; 123 lines short but focused (cf. #13=128) — audited coverage-complete. **Recipes at 16** (data-fetching 3 / state-management 2 / performance 1 / forms-and-ux 1 / auth 3 [complete] / routing 1 / **micro-frontends 4**). One long-tail MFE candidate remains: `cross-remote-state-coupling` (the shared-store-across-remotes anti-pattern → event/contract communication; 37's `#cross-mfe-state-is-the-anti-pattern` + 38's cross-remote-state note both forward-ref it). Next up: `cross-remote-state-coupling` to close the MFE track, OR return to the earlier backlog (routing pair `back-button-scroll`/`lazy-route-flashes-blank`; testing/ or real-time/ openers). SSR/RSC recipes still parked per Huy.
- **Session 6 — micro-frontends recipe #17 (cross-remote state); MFE recipe track COMPLETE.** `recipes/micro-frontends/cross-remote-state-coupling.md` (140 lines) delivered as recipe #17, **closing the `micro-frontends/` track (5 recipes: singleton #13 / isolation #14 / governance #15 / styling #16 / state #17).** `primary_concept: architecture/micro-frontends` (37 owns the anti-pattern), `difficulty: advanced`. Scenario = a `shared` Zustand singleton cart store read+written by all remotes rots 3 ways: shape coupling (a rename silently breaks un-rebuilt readers → `$NaN`; = #15 version-skew but on ad-hoc-read state), mount-order races (read-before-write → 0-then-flicker), refactor lockstep (every store change a coordinated release → independence is "cosplay"). Escaped QA: integrated dev = all-current + predictable mount order. Fix arc: name it (shared mutable store = monolith coupling minus atomic-refactor safety; re-creates funnel problems across an un-atomic boundary — cites 37 `#cross-mfe-state-is-the-anti-pattern` + 29 `#the-placement-funnel`; the cart IS server state — cites zustand-goes-stale `#stage-1--name-the-category-error`) → reject strict-singleton (reliable coupling still coupled) / version-the-store-package (documented not removed) → **versioned event + each remote owns its read** (server truth in each remote's Query cache; actor writes-server-then-emits `cart:changed` carrying "changed" not data → checkout refactors internals freely; listeners invalidate — cites 27 `#real-world-patterns`/`#stage-4--a-mutation-with-the-server-as-source-of-truth`) → funnel-the-rest across the boundary (navigational→URL cites params-out-of-sync `#stage-3--read-the-url-dont-store-it`; cross-cutting client→read-mostly kernel contract cites 37 `#the-shared-kernel-and-the-integration-contract`; local→local). 5 variations, 12 pitfalls + real when-NOT (one-team/build-time → normal app state via funnel). Verify-the-loop: add→event→refetch (no shared store) / checkout-refactors-internals→header works / late-mount→fetches current. Real code (versioned `EventTarget` bus + per-remote Query+invalidate + write-server-then-emit). **Raised first-pass self-audit caught one broken link — in progress.md itself:** the recipe-#16 log entry had a stray markdown link `[CSS Modules](../../ecosystem/…)` using recipe-relative paths that don't resolve from progress.md's root location → converted to the log's backtick style (the anchor pass now covers the maintainer log too). The recipe file itself was clean. **Inferred/surfaced:** `primary_concept: architecture/micro-frontends`; `difficulty: advanced`; `related` = 37 + 29 + 27 concepts + zustand-goes-stale (other-track sibling, same lesson within one app) + version-skew (same-track sibling); 140 lines. **Recipes at 17** (data-fetching 3 / state-management 2 / performance 1 / forms-and-ux 1 / auth 3 [complete] / routing 1 / **micro-frontends 5 [COMPLETE]**). **Wave 5 fully delivered: 2 concept articles (37+38) + the complete 5-recipe micro-frontends track.** Two full tracks now complete (auth 3, micro-frontends 5). Next up: back to the earlier backlog — routing pair (`back-button-scroll`/`lazy-route-flashes-blank`, both ref'd by #10, closes routing), OR `testing/`-track opener (gated by 31), OR `real-time/`-track opener (StrictMode double-connect). SSR/RSC recipes still parked per Huy.
- **Session 6 — ssr-and-rsc recipe track OPENED (SSR/RSC unparked by Huy).** `recipes/ssr-and-rsc/hydration-mismatch.md` (138 lines) delivered as recipe #18 — opens track #9, **pays article 33's See-also forward-ref.** New `recipes/ssr-and-rsc/` folder. `primary_concept: server/ssr-and-hydration` (25 owns the fix patterns), `difficulty: intermediate`. Scenario = SSR product page with a locale date + "3 min ago" relative time + `localStorage` theme → prod `Hydration failed` flood + flash (React 19 discards + client-re-renders the mismatched subtree → SSR lost + jolt); 3 divergences (render-time-gap, datacenter-UTC-vs-user-TZ/locale, server-can't-read-localStorage). Escaped QA: localhost = same machine (same TZ/locale, ms gap). Fix arc: name it (hydration reuses DOM + attaches handlers, no first-paint reconcile → first render must be byte-identical; the mismatch is environment-divergent data in render — cites 25 `#the-agreement-contract-and-why-mismatches-are-expensive` + `#the-anti-pattern-that-guarantees-a-mismatch`) → categorize (server-deterministic vs client-only) + reject blanket `suppressHydrationWarning` → make deterministic values match (server-computed ISO + server-decided locale/timezone as props → identical `Intl.DateTimeFormat`; theme via **cookie** not localStorage — cites 33 `#real-world-patterns`; `useId` — cites 36 `#useid-and-why-not-a-counter`) → gate client-only bits (render-same-then-effect — cites 25 `#pattern-1--render-the-same-on-both-passes-then-update-in-an-effect`; `useSyncExternalStore`+`getServerSnapshot` — cites 25 `#pattern-2--usesyncexternalstore-with-getserversnapshot` + 19). 5 variations, 12 pitfalls + real when-NOT (intended-per-env → mount-gate correct; not-SSR → no contract). Verify-the-loop: server `TZ=UTC`+non-US `LANG` → US client → no warning/flash. Real code (server-locale date, cookie theme, mount-gate `PostedAt`). **Article 25 owns the patterns so thoroughly (it has `#pattern-1`/`#pattern-2`/`#the-agreement-contract`/`#the-anti-pattern`) that the recipe stays symptom-first and cites-not-reteaches** — the right division. Raised first-pass self-audit found no shipping bug. **Inferred/surfaced:** `primary_concept: server/ssr-and-hydration` (framework-agnostic hydration, not the Next-specific 33); `difficulty: intermediate`; `related` = 25 + 33 + 19 + 21 concepts (no recipe siblings — opener; 2 planned siblings in See-also). **Recipes at 18** (data-fetching 3 / state-management 2 / performance 1 / forms-and-ux 1 / auth 3 [complete] / routing 1 / micro-frontends 5 [complete] / **ssr-and-rsc 1 [opened]**). Next up: continue ssr-and-rsc (`use-client-sprawl` — the RSC boundary bug; or `server-action-stale-data` — the `updateTag`/`revalidateTag` failure, both pay 33's remaining See-also debts), OR the routing pair / testing / real-time openers.
- **Session 6 — ssr-and-rsc recipe #19 (RSC boundary).** `recipes/ssr-and-rsc/use-client-sprawl.md` (153 lines) delivered as recipe #19. `primary_concept: server/server-components` (26 owns the boundary model — deliberate parallel to hydration-mismatch→25), `difficulty: intermediate`. Scenario = `"use client"` added high (page/barrel/layout) for one button's `onClick` → whole subtree + its heavy libs (markdown/i18n/highlighter) ship to the client (~90KB→~400KB), server data-fetch + secrets lost; page works perfectly. Escaped QA: no functional bug; `"use client"` always silences the error; compounds silently (nobody runs the bundle analyzer). Fix arc: name it (`"use client"` = a **boundary**, transitive downward; server is default, push the opt-out DOWN to the interactive leaf — cites 26 `#the-module-graph-split--where-zero-bundle-comes-from` + `#why-server-components-cant-be-interactive`) → reject the 3 reflexes (barrel poisons re-exports; page/layout → whole route client; fix-hook-error-on-ancestor → mark the leaf; diagnose via bundle analyzer, cites 34 `#real-world-patterns`) → push boundary to leaf (async server `ProductPage` + `"use client"` `FavoriteButton`; heavy libs stay server) → **slot pattern** (client wrapper holds server `children`/props as RSC payload → stay server; cites 26 `#stage-2--a-server-component-inside-a-client-component-the-slot`). 5 variations (slot, barrel-poisoning, move-data-fetch-server, `"use client"`-vs-`"use server"`, thin-wrapper-for-3rd-party), 12 pitfalls + real when-NOT (thoroughly-interactive → high boundary honest; not-RSC → no boundary). Verify-the-loop: bundle analyzer before/after → heavy libs gone, `"use client"` count at leaves. Real code (server ProductPage + client FavoriteButton + `ClientTabs` slot). **Article 26 owns the slot + module-graph-split so the recipe cites-not-reteaches** (same clean division as #18↔25). Raised first-pass self-audit found no shipping bug. **Inferred/surfaced:** `primary_concept: server/server-components` (the boundary model this misuses, not the Next-specific 33); `difficulty: intermediate`; `related` = 26 + 33 + 34 + 27 concepts + hydration-mismatch sibling; 153 lines. **Recipes at 19** (data-fetching 3 / state-management 2 / performance 1 / forms-and-ux 1 / auth 3 [complete] / routing 1 / micro-frontends 5 [complete] / **ssr-and-rsc 2**). One ssr-and-rsc candidate remains: `server-action-stale-data` (the `updateTag`/`revalidateTag`/`revalidatePath` revalidation failure — pays 33's last See-also debt; closes the SSR/RSC track). Next up: `server-action-stale-data` to close ssr-and-rsc, OR the routing pair / testing / real-time openers.
- **Session 6 — ssr-and-rsc recipe #20 (revalidation); ssr-and-rsc track COMPLETE.** `recipes/ssr-and-rsc/server-action-stale-data.md` (129 lines) delivered as recipe #20, **closing the `ssr-and-rsc/` track (hydration #18 / use-client-sprawl #19 / server-action-stale-data #20) and paying article 33's last See-also debt.** `primary_concept: server/nextjs-and-rsc-in-practice` (33 owns the caching/invalidation model) — this **completes the 25/26/33 primary-concept symmetry** across the three ssr recipes (hydration→25, use-client→26, revalidation→33). `difficulty: intermediate`. Scenario = admin edits price via Server Action, product cached `use cache`+`cacheTag('product-'+id)`, action writes DB + succeeds → price unchanged ("saving doesn't work" — the cache serves stale); escalating near-misses (revalidatePath-wrong-path / revalidateTag-wrong-tag-silent-no-op / right-tag-but-stale-while-revalidate). Escaped QA: **doesn't repro in `next dev`** (dev ≠ prod caching → `next build && next start` only); action succeeds → tests pass. Fix arc: name it (mutation = write + invalidate; opt-in caching → 2nd half skipped; test the built app — cites 33 `#step-2--a-server-action-that-mutates-and-revalidates`) → match tag to `cacheTag` (one `productTag` helper, no drift; reject wrong-path/wrong-tag) → **`updateTag` (Server-Action-only, immediate read-your-own-writes) vs `revalidateTag(tag,'max')` (stale-while-revalidate)** — the wrong choice IS the "saved and nothing happened" bug → don't-forget-the-client-Query-cache (RSC→Query bridge = 2 caches; `invalidateQueries` — cites 33 `#step-3--the-rsc--tanstack-query-bridge` + 27 `#stage-4--a-mutation-with-the-server-as-source-of-truth`). 5 variations (invalidation-trio, one-tag-helper, client-cache, optimistic-bridges-window cites 23 `#useoptimistic--a-transition-scoped-overlay`, test-with-build), 12 pitfalls + real when-NOT (uncached→nothing to revalidate; not-RSC→ordinary invalidateQueries). Verify-the-loop: `next build && next start` → admin immediate (updateTag) / 2nd visitor next-load (revalidate) / client cache invalidated. Real code (cached+tagged getProduct, savePrice+updateTag, client invalidateQueries). **Article 33 owns the invalidation model so the recipe cites-not-reteaches** (consistent #18↔25 / #19↔26 / #20↔33). Raised first-pass self-audit found no shipping bug. **Inferred/surfaced:** `primary_concept: server/nextjs-and-rsc-in-practice`; `difficulty: intermediate` (borderline advanced — updateTag/revalidateTag + dev-vs-prod + client-cache depth — chose intermediate for ssr-track consistency); `related` = 33 + 27 + 23 concepts + hydration + double-submit recipes; 129 lines (focused; the fix is match-the-tag + right-mechanism). **Recipes at 20** (data-fetching 3 / state-management 2 / performance 1 / forms-and-ux 1 / auth 3 [complete] / routing 1 / micro-frontends 5 [complete] / **ssr-and-rsc 3 [complete]**). **Three full tracks now complete: auth (3), micro-frontends (5), ssr-and-rsc (3).** Milestone: 20 recipes; all forward-ref debts from articles 33/34/37/38 + recipes 9/10 now paid except the performance-hub pair. Next up: routing pair (`back-button-scroll`/`lazy-route-flashes-blank`, ref'd by #10 — closes routing), `performance/` hub recipes (pays 34's `route-splitting-bundle`/`virtualization`/`lcp` debts), `testing/` opener (gated 31), or `real-time/` opener (StrictMode double-connect).